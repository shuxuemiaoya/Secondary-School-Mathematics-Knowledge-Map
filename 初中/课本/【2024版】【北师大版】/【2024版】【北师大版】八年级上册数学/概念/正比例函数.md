# 正比例函数

**正比例函数**：一般地，形如 $y = kx$（$k$ 为常数，$k \neq 0$）的函数，叫作正比例函数。它是 $b = 0$ 时的一次函数。

**图象**：正比例函数 $y = kx$ 的图象是一条经过原点 $(0, 0)$ 的直线。

**增减性**：
- 当 $k > 0$ 时，$y$ 的值随着 $x$ 值的增大而增大；
- 当 $k < 0$ 时，$y$ 的值随着 $x$ 值的增大而减小。

**|k| 的意义**：$|k|$ 越大，直线越陡峭（离 $x$ 轴越远），函数值变化越快。
