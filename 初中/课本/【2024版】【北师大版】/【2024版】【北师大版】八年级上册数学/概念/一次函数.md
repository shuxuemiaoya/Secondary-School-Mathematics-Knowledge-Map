# 一次函数

**一次函数**：如果两个变量 $x, y$ 之间的对应关系可以表示成

$$
y = kx + b \quad (k, b \text{ 为常数, } k \neq 0)
$$

的形式，那么称 $y$ 是 $x$ 的一次函数。

**性质**：对一次函数而言，自变量每增加 1，函数值就增加 $k$，函数值的变化是"均匀"的。

**特殊情况**：当 $b = 0$ 时，$y = kx$，称 $y$ 是 $x$ 的正比例函数。
