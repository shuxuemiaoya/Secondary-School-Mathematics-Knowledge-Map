# 函数

**函数**：一般地，如果在一个变化过程中有两个变量 $x$ 和 $y$，并且对于变量 $x$ 的每一个值，变量 $y$ 都有唯一的值与它对应，那么我们称 $y$ 是 $x$ 的函数（function），其中 $x$ 是自变量。

**函数的三种表示方式**：
- 表格
- 关系式（解析式）
- 图象

**函数值**：对于自变量在可取值范围内的一个确定的值 $a$，函数有唯一确定的对应值，这个对应值称为当自变量等于 $a$ 时的函数值。
