![](0ff520d8ba30fa47c22e54760f991d5f.png)第07讲 5拓展三：利用导数研究函数的零点（方程的根）
### 一、知识点归纳
1、函数的零点
（1）函数零点的定义：对于函数$y = f ( x )$，把使$f ( x ) = 0$的实数$x$叫做函数$y = f ( x )$的零点．
（2）三个等价关系
方程$f ( x ) = 0$有实数根$\Leftrightarrow$函数$y = f ( x )$的图象与$x$轴有交点的横坐标$\Leftrightarrow$函数$y = f ( x )$有零点．
2、函数零点的判定
如果函数$y = f ( x )$在区间$\lbrack a , b \rbrack$上的图象是连续不断的一条曲线，并且有$f ( a ) \cdot f ( b ) < 0$，那么函数$y = f ( x )$在区间$( a , b )$内有零点，即存在$c \in ( a , b )$，使得$f ( c ) = 0$，这个$c$也就是$f ( x ) = 0$的根．我们把这一结论称为函数零点存在性定理．
注意：单调性+存在零点=唯一零点
### 二、题型精讲
##### 题型01判断、证明或讨论函数零点（方程的根）的个数
##### 1．（2022上·江苏·高三校联考阶段练习）已知函数$f ( x ) = \text{cos} x - x^{2}$.
(1)设$g ( x ) = f^{'} ( x )$，求$g ( x )$在区间$\left\lbrack \frac{\pi}{4} , \pi \right\rbrack$上的最值；
(2)讨论$f ( x )$的零点个数.
<span class="fake-tag">答案</span>(1)最大值为$- \frac{\pi}{2} - \frac{\sqrt{2}}{2}$，最小值为$- 2 \pi$
(2)$f ( x )$在$\mathbf{R}$上有两个零点
<span class="fake-tag">详解</span>（1）因为$g ( x ) = f^{'} ( x ) = - 2 x - \text{sin} x , g^{'} ( x ) = - 2 - \text{cos} x < 0$，
所以$g ( x )$在区间$\left\lbrack \frac{\pi}{4} , \pi \right\rbrack$上单调递减，
所以当$x = \frac{\pi}{4}$时，$g ( x )$取最大值$g \left( \frac{\pi}{4} \right) = - \frac{\pi}{2} - \frac{\sqrt{2}}{2}$；
当$x = \pi$时，$g ( x )$取最小值$g ( \pi ) = - 2 \pi$.
（2）先讨论$f ( x )$在$(0 , + \infty )$上的零点个数，
由（1）可知，$f^{'} ( x )$在$( 0 , + \infty )$上递减，$f^{'} ( x ) < f^{'} ( 0 ) = 0$，
所以$f ( x )$在$( 0 , + \infty )$上递减，因为$f ( 0 ) = 1 > 0 , f \left( \frac{\pi}{2} \right) = - \left( \frac{\pi}{2} \right)^{2} < 0$，
所以$f ( x )$在$(0 , + \infty )$上有唯一零点，
又因为$f ( - x ) = \text{cos} ( - x ) - ( - x )^{2} = \text{cos} x - x^{2} = f ( x )$，
所以$f ( x )$是偶函数，所以$f ( x )$在$\mathbf{R}$上有两个零点.
##### 2．（2022下·山东青岛·高二山东省莱西市第一中学校考阶段练习）已知函数$f ( x ) = x - 1 - m e^{x + 1} = 0 , m \in R$，讨论函数$f ( x )$的零点的个数*.*
<span class="fake-tag">答案</span>答案见解析
<span class="fake-tag">详解</span>由$f ( x ) = x - 1 - m e^{x + 1} = 0$得$m = \frac{x - 1}{e^{x + 1}}$， 设$g ( x ) = \frac{x - 1}{e^{x + 1}}$，
则$g \left( x^{'} \right) = \frac{e^{x + 1} - ( x - 1 ) e^{x + 1}}{( e^{x + 1} )^{2}} = \frac{2 - x}{e^{x + 1}}$，
令$g \left( x^{'} \right) > 0$，得$x < 2$，此时$g ( x )$单调递增，
令$g \left( x^{'} \right) < 0$，得$x > 2$，此时$g ( x )$单调递减，
即当时，*g*(*x*)取得极大值即$g ( 2 ) = \frac{1}{e^{3}} > 0$，
由$x < 2$，$g ( x )$单调递增，$g ( 1 ) = \frac{1 - 1}{e^{x + 1}} = 0$可得$g ( x )$与*x*轴只有一个交点，
由$x > 2$，$g ( x )$单调递减，$g ( x ) = \frac{x - 1}{e^{x + 1}} > 0$可得$g ( x )$与*x*轴没有交点，
画出$g ( x )$的大致图象如图， 可得*m*≤0或*m*=$\frac{1}{e^{3}}$时，$f ( x )$有1个零点；
当0\<*m*\<$\frac{1}{e^{3}}$时，$f ( x )$有2个零点；当*m*\>$\frac{1}{e^{3}}$时，$f ( x )$没有零点.
综上所述，当*m*≤0或*m*=$\frac{1}{e^{3}}$时，$f ( x )$有1个零点；
当0\<*m*\<$\frac{1}{e^{3}}$时，$f ( x )$有2个零点；
当*m*\>$\frac{1}{e^{3}}$时，$f ( x )$没有零点.
![@@@8678106b29f34240b0b6840a9b2a5972|354](ed9c6f7df9b3e4a285c779d26f1b55ec.png)
##### 3．（2022下·山东聊城·高二统考期末）已知函数$f ( x ) = \frac{1}{3} x^{3} + \frac{1}{2} x^{2} - a x + 1 \left( a \in \mathbf{R} \right)$，在$x = 0$处切线的斜率为-2.
（1）求$a$的值及$f ( x )$的极小值；
（2）讨论方程$f ( x ) = m \left( m \in \mathbf{R} \right)$的实数解的个数.
<span class="fake-tag">答案</span>（1）$a = 2$，极小值为$- \frac{1}{6}$；（2）答案见解析．
<span class="fake-tag">详解</span>解：（1）$f^{'} ( x ) = x^{2} + x - a$，
因为在$x = 0$处切线的斜率为-2，所以$f^{'} ( 0 ) = - 2$，则$a = 2$.
$f^{'} ( x ) = x^{2} + x - 2 = ( x + 2 ) ( x - 1 )$，令$f^{'} ( x ) = 0$，解得$x = - 2$或$x = 1$，
当*x*变化时，$f^{'} ( x )$，$f ( x )$变化情况如下：

| *x*           | $( - \infty , - 2 )$ | -2             | $( - 2 , 1 )$ | 1               | $( 1 , + \infty )$ |
| ------------- | -------------------- | -------------- | ------------- | --------------- | ------------------ |
| $f^{'} ( x )$ | $+$                  | 0              | $-$           | 0               | $-$                |
| $f ( x )$     | 单调递增                 | $\frac{13}{3}$ | 单调递减          | $- \frac{1}{6}$ | 单调递增               |
故$f ( x )$的极小值为$f ( 1 ) = - \frac{1}{6}$.
（2）由（1）知，$f ( x )$在$( - \infty , - 2 )$上单调递增，$[ - 2 , 1 ]$上单调递减，$( 1 , + \infty )$上单调递增.当$x \rightarrow + \infty$时，$f ( x ) \rightarrow + \infty$；当$x \rightarrow - \infty$时，$f ( x ) \rightarrow - \infty$.
当$m > \frac{13}{3}$或$m < - \frac{1}{6}$时，方程$f ( x ) = m$有1个实数解；
当或$m = - \frac{1}{6}$时，方程$f ( x ) = m$有2个实数解
当$- \frac{1}{6} < m < \frac{13}{3}$时，方程$f ( x )$有3个实数解.
##### 4．（2023上·云南·高三校联考阶段练习）已知$g ( x ) = x e^{x} - a \left( \ln x + x \right)$．
(1)当$a = 1$时，求$g ( x )$在$( 1 , + \infty )$上的单调性；
(2)若$h ( x ) = x e^{x}$，令$f ( x ) = h^{'} ( x )$，讨论方程$f ( x ) = m \left( m \in \mathbf{R} \right)$的解的个数．
<span class="fake-tag">答案</span>(1)$g ( x )$在$( 1 , + \infty )$上递增
(2)答案见解析
<span class="fake-tag">详解</span>（1）因为$g ( x ) = x e^{x} - a \left( \ln x + x \right)$
所以当$a = 1$时，$g ( x ) = x e^{x} - \ln x - x$，
所以$g^{'} ( x ) = ( x + 1 ) e^{x} - \frac{1}{x} - 1 = ( x + 1 ) \left( e^{x} - \frac{1}{x} \right)$，
则当$x > 1$时，$x + 1 > 2$，$e^{x} > e$，$0 < \frac{1}{x} < 1$可得$g^{'} ( x ) > 0$，
所以$g ( x )$在$( 1 , + \infty )$上递增．
（2）因为$h ( x ) = x e^{x}$，$h^{'} ( x ) = ( x + 1 ) e^{x}$，
所以$f ( x ) = h^{'} ( x ) = ( x + 1 ) e^{x}$，，
令$f^{'} ( x ) = 0$，解得$x = - 2$，
所以$f ( x )$在区间$( - \infty , - 2 )$上单调递减，在区间$( - 2 , + \infty )$上单调递增．
当$x = - 2$时，$f ( x )$有极小值$f ( - 2 ) = - \frac{1}{e^{2}}$．
令$f ( x ) = 0$，解得$x = - 1$．令$x = 0$，可得$f ( 0 ) = 1 \times e^{0} = 1$，
当$x < - 1$时，$f ( x ) < 0$；当$x > - 1$时，$f ( x ) > 0$．
所以，$f ( x )$的图像经过特殊点$A \left( - 2 , - \frac{1}{e^{2}} \right)$，$B ( - 1 , 0 )$，$C ( 0 , 1 )$．
当$x \rightarrow - \infty$时，$e^{x} \rightarrow 0$，从而$f ( x ) \rightarrow 0$；
当时，，$e^{x} \rightarrow + \infty$，从而$f ( x ) \rightarrow + \infty$．
根据以上信息，我们画出$f ( x )$的大致图像如图所示．
![@@@3fb75692-e643-47a6-af22-2ccc21668e3f|476](d627dc6a19c40ce0a80b1709f8799f38.png)
方程$f ( x ) = m \left( m \in \mathbf{R} \right)$的解的个数为函数的图像与直线$y = m$的交点个数．
所以，关于方程$f ( x ) = m \left( m \in \mathbf{R} \right)$的解的个数有如下结论：
当$m < - \frac{1}{e^{2}}$时，解为0个；
当$m = - \frac{1}{e^{2}}$或$m \geq 0$时，解为1个；
当$- \frac{1}{e^{2}} < m < 0$时，解为2个．
##### 5．（2023·四川绵阳·统考模拟预测）函数$f ( x ) = \left( 2 x^{2} + m \right) ( x - m + 2 )$．
(1)若$f ( x )$为奇函数，求实数$m$的值；
(2)已知$f ( x )$仅有两个零点，证明：函数$y = f ( x ) - 3$仅有一个零点．
<span class="fake-tag">答案</span>(1)$2$
(2)证明见解析
<span class="fake-tag">详解</span>（1）解：因为$f ( x ) = ( 2 x^{2} + m ) ( x - m + 2 ) = 2 x^{3} - 2 ( m - 2 ) x^{2} + m x - m ( m - 2 )$为奇函数，
所以可知$f ( x )$的定义域为$R$，且$f ( x ) + f ( - x ) = 0$，
即$2 x^{3} - 2 ( m - 2 ) x^{2} + m x - m ( m - 2 ) + 2 ( - x )^{3} - 2 ( m - 2 ) ( - x )^{2} + m ( - x ) - m ( m - 2 ) = 0$，
即$- 2 ( m - 2 ) x^{2} - m ( m - 2 ) = 0$，
所以$\begin{cases}- 2 ( m - 2 ) = 0 \\ - m ( m - 2 ) = 0\end{cases}$，解得$m = 2$.
（2）证明：①当$m > 0$时，$2 x^{2} + m > 0$，
所以函数$f ( x ) = ( 2 x^{2} + m ) ( x - m + 2 )$不可能有两个零点，此时不合题意；
②当$m < 0$时，令，解得：$x = \pm \sqrt{- \frac{m}{2}}$或$m - 2$，
又因$\sqrt{- \frac{m}{2}} > 0 , - \sqrt{- \frac{m}{2}} < 0 , m - 2 < 0$，
则要使得*f*（*x*）仅有两个零点，则$m - 2 = - \sqrt{- \frac{m}{2}}$，
即$2 m^{2} - 7 m + 8 = 0$，此方程无解，此时不合题意；
③当$m = 0$时，即$f ( x ) = 2 x^{3} + 4 x^{2}$，
令，解得$x = 0$或$x = - 2$，符合题意，所以$m = 0$.
令$h ( x ) = f ( x ) - 3 = 2 x^{3} + 4 x^{2} - 3$，则$h^{'} ( x ) = 6 x^{2} + 8 x = 2 x ( 3 x + 4 )$，
令$h^{'} ( x ) > 0$，解得：$x > 0$或$x < - \frac{4}{3}$，令解得：$- \frac{4}{3} < x < 0$，
故在$( - \infty ， - \frac{4}{3} )$，$( 0 ， + \infty )$上递增，在$( - \frac{4}{3} ， 0 )$上递减，
又$h ( - \frac{4}{3} ) = - \frac{17}{27} < 0$，
故函数$y = f ( x ) - 3$仅有一个零点．
![@@@7a3a011d-48b8-49d3-9667-28df216e38dc](97e5da98368d48bf2adc4629ba36bb0d.png)
##### 题型02利用最值（极值）研究函数零点（方程的根）问题
##### 1．（2023上·北京·高三北京四中校考期中）已知函数$f ( x ) = \frac{2 \ln x}{x} + a \left( x^{2} - 1 \right)$.
(1)当$a = 0$时，求$f ( x )$的极值；
(2)当$a = 1$时，求$f ( x )$在$(1 , + \infty )$上的最小值；
(3)若$f ( x )$在$( 1 , e )$上存在零点，求$a$的取值范围.
<span class="fake-tag">答案</span>(1)极大值为$\frac{2}{e}$，没有极小值.
(2)0
(3)$\left( - 1 , \frac{- 2}{e^{3} - e} \right)$
<span class="fake-tag">详解</span>（1）当$a = 0$时，$f ( x ) = \frac{2 \ln x}{x}$，定义域：$( 0 , + \infty )$，$f^{'} ( x ) = \frac{2 - 2 \ln x}{x^{2}}$，
令$f^{'} ( x ) = 0$，则$x = e$，$x$变化时，$f^{'} ( x )$，$f ( x )$的变化情况如下表：

| $x$           | $( 0 , e )$ | $e$              | $( e , + \infty )$ |
| ------------- | ----------- | ---------------- | ------------------ |
| $f^{'} ( x )$ | $+$         | 0                | $-$                |
| $f ( x )$     | 单调递增        | 极大值$\frac{2}{e}$ | 单调递减               |
则$f ( x )$的极大值为：$f ( e ) = \frac{2}{e}$，$f ( x )$没有极小值；
（2）当$a = 1$时，$f ( x ) = \frac{2 \ln x}{x} + \left( x^{2} - 1 \right)$，定义域：$(1 , + \infty)$，
$f^{'} ( x ) = \frac{2 - 2 \ln x}{x^{2}} + 2 x = \frac{2 \left( 1 - \ln x + x^{3} \right)}{x^{2}}$，
令$g ( x ) = 1 - \ln x + x^{3}$，定义域：$( 1 , + \infty )$，$g^{'} ( x ) = - \frac{1}{x} + 3 x^{2} = \frac{3 x^{2} - 1}{x} > 0$，
则$g ( x )$在$( 1 , + \infty )$上是增函数，则$g {( x ) ( 1 )}_{\min}$，所以$f^{'} ( x ) > 0$，
即$f ( x )$在$(1 , + \infty )$上是增函数，则$f {( x ) ( 1 )}_{\min}$.
（3）$f ( x ) = \frac{2 \ln x}{x} + a \left( x^{2} - 1 \right)$，定义域：$( 1 , e )$，
$f^{'} ( x ) = \frac{2 - 2 \ln x}{x^{2}} + 2 a x = \frac{2 \left( 1 - \ln x + a x^{3} \right)}{x^{2}}$，
令$g ( x ) = a x^{3} - \ln x + 1$，定义域：$( 1 , e )$，$g^{'} ( x ) = 3 a x^{2} - \frac{1}{x} = \frac{3 a x^{3} - 1}{x}$，
（1）当$a < 0$时，$g^{'} ( x ) < 0$，则$g ( x )$在$( 1 , e )$上是减函数，则$g ( x ) < g ( 1 ) = a + 1$，
当$a \leq - 1$时，$f^{'} ( x ) < 0$，则$f ( x )$在$( 1 , e )$上是减函数，$f {( x ) ( 1 )}_{\max}$，不合题意；
当$- 1 < a < 0$时，$g ( 1 ) = a + 1 > 0$，$g ( e ) = a e^{3} < 0$，则存在$x_{0} \in ( 1 , e )$，使$g \left( x_{0} \right) = 0$，即$f^{'} \left( x_{0} \right) = 0$，
$x$变化时，$f^{'} ( x )$，$f ( x )$的变化情况如下表：

| $x$           |      | $x_{0}$                     | $\left( x_{0} , e \right)$ |
| ------------- | ---- | --------------------------- | -------------------------- |
| $f^{'} ( x )$ | $+$  | 0                           | $-$                        |
| $f ( x )$     | 单调递增 | 极大值$f \left( x_{0} \right)$ | 单调递减                       |
则$f \left( x_{0} \right) > f ( 1 ) = 0$，只需$f ( e ) = \frac{2}{e} + a \left( e^{2} - 1 \right) < 0$，即$a < \frac{- 2}{e^{3} - e}$；
（2）当$a = 0$时，由（1）知$f ( x )$在$( 1 , e )$上是增函数，$f ( x ) > f ( 1 ) = 0$，不合题意；
（3）当$a > 0$时，$y = \frac{2 \ln x}{x}$在$( 1 , e )$上是增函数，$y = a \left( x^{2} - 1 \right)$在$( 1 , e )$上是增函数，
则$f ( x )$在$( 1 , e )$上是增函数，$f ( x ) > f ( 1 ) = 0$，不合题意，
综上所述，$a$的取值范围是$\left( - 1 , \frac{- 2}{e^{3} - e} \right)$.
##### 2．（2023上·福建福州·高三校联考期中）已知函数$f ( x ) = x^{3} - \frac{9}{2} x^{2} + 6 x - a$（$a \in R$）.
(1)求$f ( x )$在$[ - 2 , 3 ]$上的最大值；
(2)若函数$f ( x )$恰有三个零点，求*a*的取值范围.
<span class="fake-tag">答案</span>(1)$\frac{9}{2} - a$
(2)$\left( 2 , \frac{5}{2} \right)$
（2）根据函数的单调性，求得其极大值和极小值，结合零点存在性定理，可得答案.
<span class="fake-tag">详解</span>（1）$f^{'} ( x ) = 3 x^{2} - 9 x + 6 = 3 ( x - 1 ) ( x - 2 )$，
可知$x \in [ - 2 , 1 ]$时，$f ( x )$单调递增，$x \in [ 1 , 2 ]$时，$f ( x )$单调递减，$x \in [ 2 , 3 ]$时，$f ( x )$单调递增，
由$f ( - 2 ) = - 38 - a$，，，$f ( 3 ) = \frac{9}{2} - a$，
则$f {( x ) ( 3 ) \frac{9}{2}}_{\max}$.
（2）由（1）知$f ( x )$在和$( 2 , + \infty )$上单调递增，在$( 1 , 2 )$上单调递减，
所以$f ( x )_{极 大} = f ( 1 ) = \frac{5}{2} - a$，$f ( x )_{极 小} = f ( 2 ) = 2 - a$，
因为$f ( x )$有三个零点，所以$\begin{cases}f ( x )_{极 大} > 0 \\ f ( x )_{极 小} < 0\end{cases}$，即$\begin{cases}\frac{5}{2} - a > 0 \\ 2 - a < 0\end{cases}$，
解得$2 < a < \frac{5}{2}$，故$a$的取值范围为$\left( 2 , \frac{5}{2} \right)$.
##### 3．（2023上·陕西·高三校联考阶段练习）已知函数$f ( x ) = a \text{ln} x - x ( a \in R )$.
(1)讨论$f ( x )$的单调性；
(2)若$f ( x )$在$\left\lbrack \frac{1}{e} , e^{2} \right\rbrack$上存2个零点，求$a$的取值范围.
<span class="fake-tag">答案</span>(1)答案见解析
(2)$\left. e , \frac{e^{2}}{2} \right.$
<span class="fake-tag">详解</span>（1）函数$f ( x )$的定义域为$( 0 , + \infty )$，且$f^{'} ( x ) = \frac{a}{x} - 1 = \frac{a - x}{x} ( x > 0 )$.
当$a \leq 0$时，$f^{'} ( x ) < 0$在$( 0 , + \infty )$上恒成立，故$f ( x )$在$( 0 , + \infty )$上单调递减；
当$a > 0$时，令$f^{'} ( x ) > 0 ( x > 0 )$，得$0 < x < a$，令$f^{'} ( x ) < 0$，得$x > a$，
所以$f ( x )$在上单调递增，在$( a , + \infty )$上单调递减.
综上所述，当$a \leq 0$时，$f ( x )$在$( 0 , + \infty )$上单调递减；当$a > 0$时，$f ( x )$在上单调递增，在$( a , + \infty )$上单调递减.
（2）若$a = 0 , f ( x ) = - x$，在$\left\lbrack \frac{1}{e} , e^{2} \right\rbrack$上无零点，不合题意；
若$a \neq 0$，由$f ( x ) = 0$，得$\frac{1}{a} = \frac{\text{ln} x}{x}$，
令$g ( x ) = \frac{\text{ln} x}{x}$，则直线$y = \frac{1}{a}$与函数$g ( x )$在$\left\lbrack \frac{1}{e} , e^{2} \right\rbrack$上的图象有两个交点，
$g^{'} ( x ) = \frac{1 - \text{ln} x}{x^{2}}$，当$\frac{1}{e} < x < e$时，$g^{'} ( x ) > 0$，当$e < x < e^{2}$时，$g^{'} ( x ) < 0$，
所以$g ( x )$在$\left\lbrack \frac{1}{e} , e \right\rbrack$上单调递增，在$\left\lbrack e , e^{2} \right\rbrack$上单调递减.
所以$g ( x )_{\text{max}} = g ( e ) = \frac{1}{e}$，
又$g \left( \frac{1}{e} \right) = - e , g \left( e^{2} \right) = \frac{2}{e^{2}}$，
所以要使直线$y = \frac{1}{a}$与$g ( x ) \left( x \in \left\lbrack \frac{1}{e} , e^{2} \right\rbrack \right)$的图象有两个交点，则$\frac{2}{e^{2}} \leq \frac{1}{a} < \frac{1}{e}$，
所以$e < a \leq \frac{e^{2}}{2}$，即实数$a$的取值范围为$\left. e , \frac{e^{2}}{2} \right.$.
##### 4．（2023上·重庆涪陵·高三重庆市涪陵高级中学校校考开学考试）已知函数$f ( x ) = \frac{1}{3} x^{3} + a x , g ( x ) = - x^{2} - a ( a \in R )$.
(1)若函数在$x \in \lbrack 1 , + \infty )$上单调递增，求$a$的最小值；
(2)若函数$G ( x ) = f ( x ) + g ( x )$的图象与有且只有一个交点，求$a$的取值范围．
<span class="fake-tag">答案</span>(1)$- 3$
(2)$( - \infty , - \frac{4}{3} ) \cup ( 0 , \infty )$
<span class="fake-tag">详解</span>（1），$F^{'} ( x ) = x^{2} + 2 x + a$，
因函数在$x \in \lbrack 1 , + \infty )$上单调递增，
所以$F^{'} ( x ) = x^{2} + 2 x + a \geq 0$在$x \in \lbrack 1 , + \infty )$恒成立，即$a \geq \left( - x^{2} - 2 x \right)_{\max}$，$a \geq - 3$，
的最小值为$- 3$．
（2）与有且只有一个交点，
即$\frac{1}{3} x^{3} - x^{2} + a x - a = a x$只有一个根，
$\therefore \frac{1}{3} x^{3} - x^{2} - a = 0$只有一个根，
令$h ( x ) = \frac{1}{3} x^{3} - x^{2}$，所以$h ( x )$的图象与$y = a$的图象只有一个交点，
$h^{'} ( x ) = x^{2} - 2 x$，令$h^{'} ( x ) > 0$，解得$x < 0$或$x > 2$，
令$h^{'} ( x ) < 0$，解得$0 < x < 2$，所以$h ( x )$在$( - \infty , 0 )$，$( 2 , + \infty )$上单调递增，$( 0 , 2 )$上单调递减，$h ( x )$的图象如下所示：
![@@@e72167af-694c-4fb2-853a-b48190a8afab|223](a18662df35bae8fe624826982ccdef52.png)
$\therefore h ( x )_{极 大 值} = h ( 0 ) = 0 , h ( x )_{极 小 值} = h ( 2 ) = - \frac{4}{3}$，
又$\because h ( x )$的图象与$y = a$的图象只有一个交点，
$\therefore a \in ( - \infty , - \frac{4}{3} ) \cup ( 0 , \infty )$.
##### 5．（2023下·辽宁·高二校联考期末）已知函数$f ( x ) = a^{x}$，$g ( x ) = \log_{a} x$，其中$a > 1$，若$h ( x ) = \frac{x^{a}}{f ( x )} ( x > 0 )$.
(1)当$a = 2$时，求$h ( x )$的单调区间；
(2)曲线$y = h ( x )$与直线$y = 1$有且仅有两个交点，求$a$的取值范围.
<span class="fake-tag">答案</span>(1)的增区间为$( 0 , \frac{2}{\text{ln} 2} )$，减区间为$( \frac{2}{\text{ln} 2}$,$+ \infty )$
(2)$( 1 , e ) \cup ( \text{e,+} \infty )$
<span class="fake-tag">详解</span>（1）$h ( x ) = \frac{x^{a}}{a^{x}} ( x > 0 )$，
当$a = 2$时，$h ( x ) = \frac{x^{2}}{2^{x}}$，
$h^{'} ( x ) = \frac{2 x \cdot 2^{x} - x^{2} \cdot 2^{x} \text{ln} 2}{( 2^{x} )^{2}} = \frac{x ( 2 - x \text{ln} 2 )}{2^{x}}$，
令$h^{'} ( x ) > 0$，得$2 - x \text{ln} 2 > 0$，即$0 < x < \frac{2}{\text{ln} 2}$，
令，得$2 - x \text{ln} 2 < 0$，即$x > \frac{2}{\text{ln} 2}$，
所以的增区间为$( 0 , \frac{2}{\text{ln} 2} )$，减区间为$( \frac{2}{\text{ln} 2}$,$+ \infty )$．
（2）$h ( x ) = \frac{x^{a}}{a^{x}} = 1 ( x > 0 )$，
所以$a^{x} = x^{a} ( x > 0 )$，
两边取对数可得$x \text{ln} a = a \text{ln} x$，
所以$\frac{\text{ln} x}{x} = \frac{\text{ln} a}{a}$，
设$k ( x ) = \frac{\text{ln} x}{x}$，
所以$k^{'} ( x ) = \frac{\frac{1}{x} \cdot x - \text{ln} x}{x^{2}} = \frac{1 - \text{ln} x}{x^{2}}$，
令$k^{'} ( x ) = 0$得$x = e$，
所以在$( 0 , e )$上，$k^{'} ( x ) > 0$，$k ( x )$单调递增，
在$( e , + \infty )$上，$k^{'} ( x ) < 0$，$k ( x )$单调递减，
所以$k ( x )_{\text{max}} = k ( e ) = \frac{1}{e}$，
又因为$k ( 1 ) = 0$，且$x > 1$时，$k ( x ) > 0$，
所以曲线$y = h ( x )$与直线$y = 1$有且仅有两个交点，
即曲线$y = k ( x )$与直线$y = \frac{\ln a}{a}$有两个交点的充分必要条件为$0 < \frac{\text{ln} a}{a} < \frac{1}{e}$，
所以$0 < k ( a ) < k ( e )$，
所以$a$的取值范围为$( 1 , e ) \cup ( \text{e,+} \infty )$．
##### 6．（2023下·广东东莞·高二统考期末）已知函数．
(1)求函数$f ( x )$在$x = 0$处的切线方程；
(2)若$x = 1$是$f ( x )$的极值点，且方程$f ( x ) - m = 0$有3个不同的实数解，求实数$m$的取值范围．
<span class="fake-tag">答案</span>(1)$2 x + y + 1 = 0$
(2)$- \frac{13}{6} < m < \frac{7}{3}$
<span class="fake-tag">详解</span>（1）因为，故$f^{'} ( x ) = x^{2} + 2 a x - 2$，
故$f^{'} ( 0 ) = - 2 , f ( 0 ) = - 1$，
故函数$f ( x )$在$x = 0$处的切线方程为$y + 1 = - 2 ( x - 0 )$，即$2 x + y + 1 = 0$
（2）由于$x = 1$是$f ( x )$的极值点，故$f^{'} ( 1 ) = 1 + 2 a - 2 = 0 , \therefore a = \frac{1}{2}$，
此时$f^{'} ( x ) = x^{2} + x - 2$，当$x < - 2$或$x > 1$时，$f^{'} ( x ) > 0$，即$f ( x )$在$( - \infty , - 2 ) , ( 1 , + \infty )$上单调递增，
当$- \text{2<} x \text{<1}$时，$f^{'} ( x ) < 0$，$f ( x )$在$( - 2 , 1 )$上单调递减
即$x = - 2$为函数的极大值点，$x = 1$是函数的极小值点，故$a = \frac{1}{2}$，
故$f ( x ) = \frac{1}{3} x^{3} + \frac{1}{2} x^{2} - 2 x - 1$，
故方程$f ( x ) - m = 0$有3个不同的实数解，即$y = f ( x ) , y = m$的图象由3个不同交点，
而$f {( x ) ( - 2 ) \frac{7}{3}}_{\max}$，$f ( x {) \frac{13}{6}}_{\min}$，
结合$f ( x ) = \frac{1}{3} x^{3} + \frac{1}{2} x^{2} - 2 x - 1$的图象，当$x \rightarrow - \infty$时，$f ( x )$可取负无穷小，
当时，$f ( x )$可取正无穷大，
![@@@f8ae7469-5653-4e97-9b06-599fc5d7b731](e45a8ee3d4d67c662735e85bb50dcd9e.png)
可得到$- \frac{13}{6} < m < \frac{7}{3}$.
##### 题型03利用数形结合法研究函数的零点（方程的根）问题
##### 1．（2023上·重庆涪陵·高三重庆市涪陵高级中学校校考开学考试）已知函数$f ( x ) = \frac{1}{3} x^{3} + a x , g ( x ) = - x^{2} - a ( a \in R )$.
(1)若函数在$x \in \lbrack 1 , + \infty )$上单调递增，求$a$的最小值；
(2)若函数$G ( x ) = f ( x ) + g ( x )$的图象与有且只有一个交点，求$a$的取值范围．
<span class="fake-tag">答案</span>(1)$- 3$
(2)$( - \infty , - \frac{4}{3} ) \cup ( 0 , \infty )$
<span class="fake-tag">详解</span>（1），$F^{'} ( x ) = x^{2} + 2 x + a$，
因函数在$x \in \lbrack 1 , + \infty )$上单调递增，
所以$F^{'} ( x ) = x^{2} + 2 x + a \geq 0$在$x \in \lbrack 1 , + \infty )$恒成立，即$a \geq \left( - x^{2} - 2 x \right)_{\max}$，$a \geq - 3$，
的最小值为$- 3$．
（2）与有且只有一个交点，
即$\frac{1}{3} x^{3} - x^{2} + a x - a = a x$只有一个根，
$\therefore \frac{1}{3} x^{3} - x^{2} - a = 0$只有一个根，
令$h ( x ) = \frac{1}{3} x^{3} - x^{2}$，所以$h ( x )$的图象与$y = a$的图象只有一个交点，
$h^{'} ( x ) = x^{2} - 2 x$，令$h^{'} ( x ) > 0$，解得$x < 0$或$x > 2$，
令$h^{'} ( x ) < 0$，解得$0 < x < 2$，所以$h ( x )$在$( - \infty , 0 )$，$( 2 , + \infty )$上单调递增，$( 0 , 2 )$上单调递减，$h ( x )$的图象如下所示：
![@@@e72167af-694c-4fb2-853a-b48190a8afab|365](a18662df35bae8fe624826982ccdef52.png)
$\therefore h ( x )_{极 大 值} = h ( 0 ) = 0 , h ( x )_{极 小 值} = h ( 2 ) = - \frac{4}{3}$，
又$\because h ( x )$的图象与$y = a$的图象只有一个交点，
$\therefore a \in ( - \infty , - \frac{4}{3} ) \cup ( 0 , \infty )$.
##### 2．（2023上·云南·高三校联考阶段练习）已知$g ( x ) = x e^{x} - a \left( \ln x + x \right)$．
(1)当$a = 1$时，求$g ( x )$在$( 1 , + \infty )$上的单调性；
(2)若$h ( x ) = x e^{x}$，令$f ( x ) = h^{'} ( x )$，讨论方程$f ( x ) = m \left( m \in \mathbf{R} \right)$的解的个数．
<span class="fake-tag">答案</span>(1)$g ( x )$在$( 1 , + \infty )$上递增
(2)答案见解析
<span class="fake-tag">详解</span>（1）因为$g ( x ) = x e^{x} - a \left( \ln x + x \right)$
所以当$a = 1$时，$g ( x ) = x e^{x} - \ln x - x$，
所以$g^{'} ( x ) = ( x + 1 ) e^{x} - \frac{1}{x} - 1 = ( x + 1 ) \left( e^{x} - \frac{1}{x} \right)$，
则当$x > 1$时，$x + 1 > 2$，$e^{x} > e$，$0 < \frac{1}{x} < 1$可得$g^{'} ( x ) > 0$，
所以$g ( x )$在$( 1 , + \infty )$上递增．
（2）因为$h ( x ) = x e^{x}$，$h^{'} ( x ) = ( x + 1 ) e^{x}$，
所以$f ( x ) = h^{'} ( x ) = ( x + 1 ) e^{x}$，，
令$f^{'} ( x ) = 0$，解得$x = - 2$，
所以$f ( x )$在区间$( - \infty , - 2 )$上单调递减，在区间$( - 2 , + \infty )$上单调递增．
当$x = - 2$时，$f ( x )$有极小值$f ( - 2 ) = - \frac{1}{e^{2}}$．
令$f ( x ) = 0$，解得$x = - 1$．令$x = 0$，可得$f ( 0 ) = 1 \times e^{0} = 1$，
当$x < - 1$时，$f ( x ) < 0$；当$x > - 1$时，$f ( x ) > 0$．
所以，$f ( x )$的图像经过特殊点$A \left( - 2 , - \frac{1}{e^{2}} \right)$，$B ( - 1 , 0 )$，$C ( 0 , 1 )$．
当$x \rightarrow - \infty$时，$e^{x} \rightarrow 0$，从而$f ( x ) \rightarrow 0$；
当时，，$e^{x} \rightarrow + \infty$，从而$f ( x ) \rightarrow + \infty$．
根据以上信息，我们画出$f ( x )$的大致图像如图所示．
![@@@3fb75692-e643-47a6-af22-2ccc21668e3f|473](d627dc6a19c40ce0a80b1709f8799f38.png)
方程$f ( x ) = m \left( m \in \mathbf{R} \right)$的解的个数为函数的图像与直线$y = m$的交点个数．
所以，关于方程$f ( x ) = m \left( m \in \mathbf{R} \right)$的解的个数有如下结论：
当$m < - \frac{1}{e^{2}}$时，解为0个；
当$m = - \frac{1}{e^{2}}$或$m \geq 0$时，解为1个；
当$- \frac{1}{e^{2}} < m < 0$时，解为2个．
##### 3．（2023下·湖南衡阳·高二校考阶段练习）已知函数$f ( x ) = \frac{2}{3} x^{3} - ( k + 1 ) x^{2} + 2 k x$，$g ( x ) = 2 k x + 1 ($其中$k \in R )$．
(1)讨论函数$f ( x )$的单调性；
(2)若方程$f ( x ) = g ( x )$有三个根，求$k$的取值范围．
<span class="fake-tag">答案</span>(1)答案见解析
(2)$\left( - \infty ， - \sqrt[3]{3} - 1 \right)$．
<span class="fake-tag">详解</span>（1）解：由题意得函数$f ( x )$的定义域为，
$f^{'} ( x ) = 2 x^{2} - 2 ( k + 1 ) x + 2 k = 2 ( x - 1 ) ( x - k )$，
$\therefore ①$当$k = 1$时，$f ' ( x ) \geq 0$，即$f ( x )$在上单调递增；
$②$当$k > 1$时，由$f ' ( x ) > 0$，得$x < 1$或$x > k$，由$f ' ( x ) < 0$，得，
$\therefore f ( x )$在$( 1 ， k )$上单调递减，在$( - \infty ， 1 )$和$( k ， + \infty )$上单调递增；
$③$当$k < 1$时，由$f ' ( x ) > 0$得$x < k$或$x > 1$，由$f^{'} ( x ) < 0$得，
$\therefore f ( x )$在$( k ， 1 )$上单调递减，在$( - \infty ， k )$和$( 1 ， + \infty )$上单调递增，
综上所述，当$k = 1$时，$f ( x )$在上单调递增；
当$k > 1$时，$f ( x )$在$( 1 ， k )$上单调递减，在$( - \infty ， 1 )$和$( k ， + \infty )$上单调递增；
当$k < 1$时，$f ( x )$在$( k ， 1 )$上单调递减，在$( - \infty ， k )$和$( 1 ， + \infty )$上单调递增；
（2）方程$f ( x ) = g ( x )$有三个根，即$\frac{2}{3} x^{3} - ( k + 1 ) x^{2} + 2 k x = 2 k x + 1$有三个根，
$\therefore \frac{2}{3} x^{3} - ( k + 1 ) x^{2} - 1 = 0$有三个根，显然$x = 0$不是方程的根，
则$k = \frac{2}{3} x - \frac{1}{x^{2}} - 1$有三个根，即$y = k$与函数$h ( x ) = \frac{2}{3} x - \frac{1}{x^{2}} - 1$的图象有三个交点，
$h^{'} ( x ) = \frac{2}{3} + \frac{2}{x^{3}}$，令$h^{'} ( x ) = 0$，可得$x = - \sqrt[3]{3}$，
由$h^{'} ( x ) > 0$，可得$x < - \sqrt[3]{3}$或$x > 0$，由$h^{'} ( x ) < 0$，可得$- \sqrt[3]{3} < x < 0$，
则$h ( x )$在$\left( - \infty ， - \sqrt[3]{3} \right)$和$( 0 ， + \infty )$上单调递增，在$\left( - \sqrt[3]{3} ， 0 \right)$上单调递减，
在$x = - \sqrt[3]{3}$处取得极大值为$h \left( - \sqrt[3]{3} \right) = - \sqrt[3]{3} - 1$，
当$x \rightarrow - \infty$时，$h ( x ) \rightarrow - \infty$，当$x \rightarrow 0^{-}$时，$h ( x ) \rightarrow - \infty$，
当时，$h ( x ) \rightarrow - \infty$，当时，$h ( x ) \rightarrow + \infty$，
如图所示：
![@@@c0c1733c-973d-454e-9891-4ab430e07072](a888aedb5973785a279413c0172cd879.png)
$\therefore$要使$y = k$与函数$h ( x ) = \frac{2}{3} x - \frac{1}{x^{2}} - 1$的图象有三个交点，
只需$k < - \sqrt[3]{3} - 1$，$\therefore k$的取值范围是$\left( - \infty ， - \sqrt[3]{3} - 1 \right)$．
##### 4．（2023上·广东江门·高三统考阶段练习）已知函数$f ( x ) = ( x + 1 ) e^{x} - a ( a \in R )$.
(1)求$f ( x )$的极值：
(2)若$f ( x )$有两个零点，求*a*的取值范围.
<span class="fake-tag">答案</span>(1)极小值为$- \frac{1}{e^{2}} - a$，无极大值
(2)$\left( - \frac{1}{e^{2}} , 0 \right)$
<span class="fake-tag">详解</span>（1）函数的定义域为$R$，
令$f^{'} ( x ) = 0$，解得$x = - 2$，
当$f^{'} ( x ) < 0$时，则$x < - 2$，
当$f^{'} ( x ) > 0$时，则，
所以$f ( x )$在区间$( - \infty , - 2 )$上单调递减，在区间$( - 2 , + \infty )$上单调递增.
所以当$x = - 2$时，$f ( x )$有极小值$f ( - 2 ) = - \frac{1}{e^{2}} - a$，无极大值.
（2）因为函数$f ( x ) = ( x + 1 ) e^{x} - a$有两个零点，
所以直线$y = a$与函数$g ( x ) = ( x + 1 ) e^{x}$有两个交点，
$g^{'} ( x ) = ( x + 2 ) e^{x}$，令$g^{'} ( x ) = 0$，解得$x = - 2$，
当$g^{'} ( x ) < 0$时，则$x < - 2$，
当$g^{'} ( x ) > 0$时，则，
所以$g ( x )$在区间$( - \infty , - 2 )$上单调递减，在区间$( - 2 , + \infty )$上单调递增.
因为，$g ( - 2 ) = - \frac{1}{e^{2}}$，
当$x < - 1$时，$g ( x ) < 0$，当$x > - 1$时，$g ( x ) > 0$
当$x \rightarrow - \infty$时，$g ( x ) \rightarrow 0$，当时，$g ( x ) \rightarrow + \infty$，$g^{'} ( x ) \rightarrow + \infty$，
所以函数的大致图象如图所示，
![@@@4e77c2f3-8221-4384-9704-51d384009978](ce18a546c5cdef4198c8254c7e48f1e2.png)
结合图象可知，当$- \frac{1}{e^{2}} < a < 0$时，$f ( x )$有两个零点，
故*a*的取值范围为$\left( - \frac{1}{e^{2}} , 0 \right)$.
##### 5．（2023上·北京·高三北京二十中校考阶段练习）已知函数$f ( x ) = \frac{1}{3} x^{3} - 6 x$，函数$g ( x ) = \frac{1}{2} x^{2} + a$，
(1)已知直线$l$是曲线在点$\left( 0 , f ( 0 ) \right)$处的切线，且$l$与曲线$y = g ( x )$相切，求$a$的值；
(2)若方程$f ( x ) = g ( x )$有三个不同实数解，求实数$a$的取值范围.
<span class="fake-tag">答案</span>(1)$18$
(2)$\left( - \frac{27}{2} , \frac{22}{3} \right)$
<span class="fake-tag">详解</span>（1）$f ( x ) = \frac{1}{3} x^{3} - 6 x$，则$f^{'} ( x ) = x^{2} - 6$，则$f^{'} ( 0 ) = - 6$，$f ( 0 ) = 0$，
故切线$l$方程为：，
$l$与曲线$y = g ( x )$相切，联立得到$\frac{1}{2} x^{2} + a = - 6 x$，即$\frac{1}{2} x^{2} + 6 x + a = 0$，
$\Delta = 3 6 - 2 a = 0$，解得.
（2）$f ( x ) = g ( x )$，即$\frac{1}{3} x^{3} - 6 x = \frac{1}{2} x^{2} + a$，则$a = \frac{1}{3} x^{3} - \frac{1}{2} x^{2} - 6 x$，
设$F ( x ) = \frac{1}{3} x^{3} - \frac{1}{2} x^{2} - 6 x$，则$F^{'} ( x ) = x^{2} - x - 6 = ( x - 3 ) ( x + 2 )$，
当$x > 3$时，$F^{'} ( x ) > 0$，函数单调递增；
当$- 2 < x < 3$时，$F^{'} ( x ) < 0$，函数单调递减；
当$x < - 2$时，$F^{'} ( x ) > 0$，函数单调递增；
$F ( 3 ) = - \frac{27}{2}$，$F ( - 2 ) = \frac{22}{3}$，画出函数图象：
![@@@2ea59f7c-040f-438b-9e19-61904550652b](b0352d5d2fadd6448b6ba358da3627e8.png)
根据图象知：$- \frac{27}{2} < a < \frac{22}{3}$.
##### 6．（2023·四川·校联考一模）已知函数$f ( x ) = x^{2} - 2 \text{ln} x$.
(1)求$f ( x )$的单调区间；
(2)令$g ( x ) = f ( x ) - x^{2} + a x$（*a*为常数），若$g ( x )$有两个零点$x_{1} , x_{2} \left( x_{1} < x_{2} \right)$，求实数*a*的取值范围.
<span class="fake-tag">答案</span>(1)$f ( x )$的单调递减区间是，单调递增区间是$( 1 , + \infty )$
(2)
<span class="fake-tag">详解</span>（1）由题意可知：$f ( x )$的定义域为$( 0 , + \infty )$， $f^{'} ( x ) = 2 x - \frac{2}{x} = \frac{2 ( x + 1 ) ( x - 1 )}{x}$，
令$f^{'} ( x ) < 0$，解得$0 < x < 1$；令$f^{'} ( x ) > 0$，解得$x > 1$；
所以$f ( x )$的单调递减区间是，单调递增区间是$( 1 , + \infty )$.
（2）由题意可知：$g ( x ) = f ( x ) - x^{2} + a x = a x - 2 \text{ln} x$，其定义域为$( 0 , + \infty )$，
则$g ( x )$有两个零点$x_{1} , x_{2}$，即$g ( x ) = 0$有两解，即$\frac{a}{2} = \frac{\text{ln} x}{x}$有两解，
令$\varphi ( x ) = \frac{\text{ln} x}{x} ( x > 0 )$，则$\varphi^{'} ( x ) = \frac{1 - \text{ln} x}{x^{2}} ( x > 0 )$.
令$\varphi^{'} ( x ) > 0$，解得$0 < x < e$；令$\varphi^{'} ( x ) < 0$，解得$x > e$；
则$\varphi ( x )$的单调递减区间是$( e , + \infty )$，单调递增区间是$( 0 , e )$，
可知$\varphi ( x ) \leq \varphi ( e ) = \frac{\text{lne}}{e} = \frac{1}{e}$，
又因为$\varphi ( 1 ) = 0$，且当$x$趋近于$+ \infty$，$\varphi ( x )$趋近于0，
要使得$\frac{a}{2} = \frac{\text{ln} x}{x}$有两解，只需$0 < \frac{a}{2} < \frac{1}{e}$，所以$0 < a < \frac{2}{e}$，
![@@@f33b9c08-8594-4e7d-ae0d-e89fe47ef27c](33d4e12781c36a8284e703bce8ea9a58.png)
故实数*a*的取值范围为.
##### 题型04构造函数研究函数零点（方程的根）问题
##### 1．（2023·全国·模拟预测）已知函数．
(1)讨论函数$f ( x )$的单调性．
(2)若关于$x$的方程$f ( x ) = \ln x - \frac{1}{2} x^{2}$有两个实数根，求实数$a$的取值范围．
<span class="fake-tag">答案</span>(1)答案见解析
(2)$\left( 0 , \frac{1}{e} \right)$
<span class="fake-tag">详解</span>（1）由题意函数$f ( x )$的定义域为．
当$a \leq 0$时，若，则$f ( x )$单调递增；
若，则$f ( x )$单调递减．
当$a > 0$时，令$f^{'} ( x ) = 0$，得$x = - 1$或$x = - \ln a$．
①当$- 1 = - \text{ln} a$时，$a = e$，则$f^{'} ( x ) \geq 0 , f ( x )$在$R$上单调递增．
②当时，$a > e$，则当$x \in ( - \infty , - \ln a )$时，$f^{'} ( x ) > 0 , f ( x )$单调递增；
当$x \in \left( - \text{ln} a , - 1 \right)$时，$f ' ( x ) < 0 , f ( x )$单调递减；
当$x \in ( - 1 , + \infty )$时，$f^{'} ( x ) > 0 , f ( x )$单调递增．
③当$- \ln a > - 1$时，$0 < a < e$，则当$x \in ( - \infty , - 1 )$时，$f^{'} ( x ) > 0 , f ( x )$单调递增；
当$x \in \left( - 1 , - \text{ln} a \right)$时，$f^{'} ( x ) < 0 , f ( x )$单调递减；
当$x \in ( - \ln a , + \infty )$时，$f^{'} ( x ) > 0 , f ( x )$单调递增．
综上，当$a \leq 0$时，$f ( x )$在$( - \infty , - 1 )$上单调递增，在$( - 1 , + \infty )$上单调递减；
当$0 < a < e$时，$f ( x )$在$( - \infty , - 1 ) , \left( - \text{ln} a , + \infty \right)$上单调递增，在$\left( - 1 , - \text{ln} a \right)$上单调递减；
当$a = e$时，$f ( x )$在$R$上单调递增；
当$a > e$时，$f ( x )$在$\left( - \infty , - \text{ln} a \right) , ( - 1 , + \infty )$上单调递增，在$\left( - \text{ln} a , - 1 \right)$上单调递减．
（2）由$f ( x ) = \ln x - \frac{1}{2} x^{2}$，得$a x e^{x} = \ln x + x$，
即．
设$g ( x ) = \text{ln} x + x , x > 0$，则$g^{'} ( x ) = \frac{1}{x} + 1 > 0$，
所以$g ( x ) = \ln x + x$为增函数，且$g ( x ) = \ln x + x$的值域为$R$．
令$t = \text{ln} x + x , t \in R$，
所以可化为$a e^{t} = t$，则$a = \frac{t}{e^{t}}$．
令$y = a , h ( t ) = \frac{t}{e^{t}}$．
因为关于$x$的方程$f ( x ) = \ln x - \frac{1}{2} x^{2}$有两个实数根，
所以直线$y = a$与函数$h ( t ) = \frac{t}{e^{t}}$的图像有两个不同的交点．
因为$h^{'} ( t ) = \frac{1 - t}{e^{t}}$，
所以当$t \in ( - \infty , 1 )$时，$h^{'} ( t ) > 0$，则$h ( t )$单调递增；
当时，$h^{'} ( t ) < 0$，则$h ( t )$单调递减．
所以$h ( t ) \leq h ( 1 ) = \frac{1}{e}$，且当$t \rightarrow + \infty$时，$h ( t ) \rightarrow 0$，
当$t \rightarrow - \infty$时，，
所以$0 < a < \frac{1}{e}$，
即实数$a$的取值范围为$\left( 0 , \frac{1}{e} \right)$．
##### 2．（2023上·湖南·高三邵阳市第二中学校联考阶段练习）已知函数$f ( x ) = 2 \text{ln} x - a x + 1 \left( a \in \mathbf{R} \right)$.
(1)讨论函数$f ( x )$的零点个数；
(2)已知函数$g ( x ) = e^{a x} - e x^{2} \left( a \in \mathbf{R} \right)$，当$0 < a < \frac{2 \sqrt{e}}{e}$时，关于$x$的方程$f ( x ) = g ( x )$有两个实根$x_{1} , x_{2} \left( x_{1} < x_{2} \right)$，求证：$x_{1} - \sqrt{e} < \frac{1}{x_{2}} - \frac{1}{\sqrt{e}}$.（注：$e = 2 . 7 1 8 2 8 \cdots$是自然对数的底数）
<span class="fake-tag">答案</span>(1)答案见解析
(2)证明见解析
<span class="fake-tag">详解</span>（1）由已知函数$f ( x )$的定义域为$( 0 , + \infty )$，
由$f ( x ) = 2 \text{ln} x - a x + 1 = 0$，得$a = \frac{2 \text{ln} x + 1}{x}$，
令函数$u ( x ) = \frac{1 + 2 \text{ln} x}{x} , u^{'} ( x ) = \frac{1 - 2 \text{ln} x}{x^{2}}$，
当$0 < x < \sqrt{e}$时，$u^{'} ( x ) > 0$，函数$u ( x )$在$\left( 0 , \sqrt{e} \right)$上单调递增，
当$x > \sqrt{e}$时，$u^{'} ( x ) < 0$，函数$u ( x )$在$\left( \sqrt{e} , + \infty \right)$单调递减，
所以$u ( x )_{\text{max}} = u \left( \sqrt{e} \right) = \frac{2}{\sqrt{e}} = \frac{2 \sqrt{e}}{e}$，
因为$u \left( \frac{1}{\sqrt{e}} \right) = 0 , \lim_{x \rightarrow 0} \frac{1 + 2 \ln x}{x} \rightarrow - \infty , \lim_{x \rightarrow + \infty} \frac{1 + 2 \ln x}{x} = 0$，
可知函数$u ( x )$的图象如下所示：
![@@@aa3eed84-ca8e-477a-8782-ba21412b443c](2929cdafb4bb76346cfbbb38a437b9d5.png)
所以当$a > \frac{2 \sqrt{e}}{e}$时，函数$f ( x )$的零点个数为0个，当$a \leq 0$或$a = \frac{2 \sqrt{e}}{e}$时，函数$f ( x )$的零点个数为1个，当$0 < a < \frac{2 \sqrt{e}}{e}$时，函数$f ( x )$的零点个数为2个.
（2）由题设方程$f ( x ) = g ( x )$，即$2 \text{ln} x - a x + 1 = e^{a x} - e x^{2}$，
所以$a x + e^{a x} = 2 \text{ln} x + e x^{2} + 1 = \text{lne} x^{2} + e^{\text{lne} x^{2}}$，
令$\varphi ( x ) = x + e^{x}$，得$\varphi ( a x ) = \varphi \left( \text{lne} x^{2} \right)$，
又$\varphi^{'} ( x ) = 1 + e^{x} > 0$在$\mathbf{R}$上恒成立，所以$\varphi ( x )$在$\mathbf{R}$上单调递增，所以$a x = \text{lne} x^{2}$，即$a x = 1 + 2 \text{ln} x$，
由已知，方程$a x = 1 + 2 \text{ln} x$有两个实根$x_{1} , x_{2} \left( x_{1} < x_{2} \right)$，
即$a = \frac{1 + 2 \text{ln} x}{x}$有两个实根$x_{1} , x_{2} \left( x_{1} < x_{2} \right)$，由（1）得$\frac{1}{\sqrt{e}} < x_{1} < \sqrt{e} < x_{2}$.
令$\frac{1}{x_{1}} = t_{1} , \frac{1}{x_{2}} = t_{2} \left( 0 < t_{2} < \frac{1}{\sqrt{e}} < t_{1} < \sqrt{e} \right)$，
所以$\begin{cases}a = t_{1} - 2 t_{1} \text{ln} t_{1} , \\ a = t_{2} - 2 t_{2} \text{ln} t_{2} ,\end{cases}$
令$h ( t ) = t - 2 t \text{ln} t ( t > 0 )$，所以$h ( t ) = a$有两个实根$t_{1} , t_{2}$，
先证$t_{1} + t_{2} > \frac{2}{\sqrt{e}}$.
因为$h^{'} ( t ) = - 1 - 2 \text{ln} t$，令$h^{'} ( t ) > 0$，解得$0 < t < \frac{1}{\sqrt{e}}$，令$h^{'} ( t ) < 0$，解得$t > \frac{1}{\sqrt{e}}$，
所以$h ( t )$在$\left( 0 , \frac{1}{\sqrt{e}} \right)$上单调递增，在$\left( \frac{1}{\sqrt{e}} , + \infty \right)$上单调递减，要证$t_{1} + t_{2} > \frac{2}{\sqrt{e}}$，即证$t_{1} > \frac{2}{\sqrt{e}} - t_{2}$，
因为$t_{1} > \frac{1}{\sqrt{e}} , \frac{2}{\sqrt{e}} - t_{2} > \frac{1}{\sqrt{e}} , h ( t )$在$\left( \frac{1}{\sqrt{e}} , + \infty \right)$上单调递减，只需证$h \left( t_{1} \right) < h \left( \frac{2}{\sqrt{e}} - t_{2} \right)$，
即证$h \left( t_{2} \right) < h \left( \frac{2}{\sqrt{e}} - t_{2} \right)$.
令$F ( t ) = h ( t ) - h \left( \frac{2}{\sqrt{e}} - t \right) \left( 0 < t < \frac{1}{\sqrt{e}} \right)$，
$F^{'} ( t ) = h^{'} ( t ) + h^{'} \left( \frac{2}{\sqrt{e}} - t \right) = - 1 - 2 \text{ln} t - 1 - 2 \text{ln} \left( \frac{2}{\sqrt{e}} - t \right) = - 2 - 2 \text{ln} \left\lbrack t \left( \frac{2}{\sqrt{e}} - t \right) \right\rbrack$，
因为$t \left( \frac{2}{\sqrt{e}} - t \right) = - t^{2} + \frac{2 t}{\sqrt{e}} \left( 0 < t < \frac{1}{\sqrt{e}} \right)$，
令$k ( t ) = - t^{2} + \frac{2 t}{\sqrt{e}} \left( 0 < t < \frac{1}{\sqrt{e}} \right)$，
可知函数$k ( t )$在$\left( 0 , \frac{1}{\sqrt{e}} \right)$上单调递增，所以$0 < t \left( \frac{2}{\sqrt{e}} - t \right) < \frac{1}{e}$，所以$\text{ln} \left\lbrack t \left( \frac{2}{\sqrt{e}} - t \right) \right\rbrack < - 1$，
所以$- 2 - 2 \text{ln} \left\lbrack t \left( \frac{2}{\sqrt{e}} - t \right) \right\rbrack > 0$，即$F^{'} ( t ) > 0$在$\left( 0 , \frac{1}{\sqrt{e}} \right)$上恒成立，
所以$F ( t )$在$\left( 0 , \frac{1}{\sqrt{e}} \right)$上单调递增，所以$F ( t ) < F \left( \frac{1}{\sqrt{e}} \right) = 0$，所以$h \left( t_{2} \right) < h \left( \frac{2}{\sqrt{e}} - t_{2} \right)$成立，
即$h \left( t_{1} \right) < h \left( \frac{2}{\sqrt{e}} - t_{2} \right)$成立，又$t_{1} > \frac{1}{\sqrt{e}} , \frac{2}{\sqrt{e}} - t_{2} > \frac{1}{\sqrt{e}}$，且$h ( t )$在$\left( \frac{1}{\sqrt{e}} , + \infty \right)$上单调递减，
所以$t_{1} > \frac{2}{\sqrt{e}} - t_{2}$，所以$t_{1} + t_{2} > \frac{2}{\sqrt{e}}$，即$\frac{1}{x_{1}} + \frac{1}{x_{2}} > \frac{2}{\sqrt{e}}$，所以$\frac{1}{x_{2}} > \frac{2}{\sqrt{e}} - \frac{1}{x_{1}}$，
所以$x_{1} - \frac{1}{x_{2}} < x_{1} + \frac{1}{x_{1}} - \frac{2}{\sqrt{e}} < \sqrt{e} - \frac{1}{\sqrt{e}}$，即$x_{1} - \sqrt{e} < \frac{1}{x_{2}} - \frac{1}{\sqrt{e}}$.
##### 3．（2023上·重庆沙坪坝·高三重庆一中校考阶段练习）已知$f ( x ) = a x e^{a x} + b e^{( b - 1 ) x}$.
(1)当$a \neq 0 , b = 0$时，求函数$f ( x )$的极值；
(2)当$a = 0$时，设$g ( x ) = f ( x ) - \text{ln} x - x$.若函数$g ( x )$在$\left(1 , e^{2} \right)$上有两个不同的零点，求$b$的取值范围.
<span class="fake-tag">答案</span>(1)极小值为$- \frac{1}{e}$，无极大值
(2)$\left[ \frac{2}{e^{2}} + 1 , \frac{1}{e} + 1 \right)$
<span class="fake-tag">详解</span>（1）当$a \neq 0 , b = 0$时，$f ( x ) = a x e^{a x}$，
由题意得：$f^{'} ( x ) = a e^{a x} + a^{2} x e^{a} = e^{a x} \left( a^{2} x + a \right)$，
当$x < - \frac{1}{a}$时，$f^{'} ( x ) < 0$，则$f ( x )$单调递减，
当$x > - \frac{1}{a}$时，$f^{'} ( x ) > 0$，则$f ( x )$单调递增，
$\therefore f ( x )_{极 小 值} = f \left( - \frac{1}{a} \right) = - \frac{1}{e}$，无极大值.
（2）当$a = 0$时，$g ( x ) = b e^{( b - 1 ) x} - \text{ln} x - x$，令$g ( x ) = 0$，
则$b e^{( b - 1 ) x} = \text{ln} x + x$在$\left. 1 , e^{2} \right.$上有两个不同的根，
而$b e^{b x} = e^{x} \left( \ln x + x \right) \Rightarrow b x e^{b x} = x e^{x} \left( \text{ln} x + x \right) \Rightarrow b x e^{b x} = e^{x + \text{ln} x} \left( \text{ln} x + x \right)$，
$\because x \in \left. 1 , e^{2} \right. , \therefore \text{ln} x + x > 0$，则$b > 0$，
令$h ( x ) = x e^{x}$，则$h ( b x ) = h \left( x + \text{ln} x \right)$，
而$h^{'} ( x ) = ( x + 1 ) e^{x} > 0$在$\left. 1 , e^{2} \right.$上恒成立，故$h ( x )$在$\left. 1 , e^{2} \right.$上单调递增，
$\therefore b x = x + \text{ln} x \Rightarrow b = 1 + \frac{\text{ln} x}{x} \Rightarrow b - 1 = \frac{\text{ln} x}{x}$，
令$u ( x ) = \frac{\text{ln} x}{x} , u^{'} ( x ) = \frac{1 - \text{ln} x}{x^{2}}$.
当$x \in ( 1 , e )$时，$u^{'} ( x ) > 0$，$u ( x )$单调递增；当时，$u^{'} ( x ) < 0$，$u ( x )$单调递减.
又$u ( 1 ) = 0 , u ( e ) = \frac{1}{e} , u \left( e^{2} \right) = \frac{2}{e^{2}}$，
所以，要使$b e^{( b - 1 ) x} = \text{ln} x + x$在$\left. 1 , e^{2} \right.$上有两个不同的根，
![@@@5ab65b64-1b1a-4b0f-8504-1452c2c91557](908381bc5d7a6a24f0c65a2338e426de.png)
由图可知，只需$\frac{2}{e^{2}} \leq b - 1 < \frac{1}{e}$，即$\frac{2}{e^{2}} + 1 \leq b < \frac{1}{e} + 1$，
$b$的取值范围为$[\frac{2}{e^{2}} + 1 , \frac{1}{e} + 1 )$.
