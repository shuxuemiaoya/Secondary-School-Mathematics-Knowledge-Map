# 专题05 含参函数的单调性讨论

# 【方法总结】

# 分类讨论思想研究函数的单调性

讨论含参函数的单调性，其本质就是讨论导函数符号的变化情况，所以讨论的关键是抓住导函数解析式中的符号变化部分，即导数的主要部分，简称导主．讨论时要考虑参数所在的位置及参数取值对导函数符号的影响，一般来说需要进行四个层次的分类：

(1)最高次幂的系数是否为0，即“是不是”；  
(2)导函数是否有变号零点，即“有没有”；  
(3)导函数的变号零点是否在函数定义域或指定区间内，即“在不在”；  
(4)导函数的变号零点之间的大小关系，即“大不大”.

牢记：十二字方针“是不是，有没有，在不在，大不大”.

# 考点一 导主一次型

# 【例题选讲】

[例 1] 已知函数 $f(x)=x-a\ln x(a\in\mathbf{R})$ ，讨论函数 $f(x)$ 的单调性.

解析 $f(x)$ 的定义域为 $(0, +\infty)$ ， $f'(x)=1-\frac{a}{x}=\frac{x-a}{x}$ ，令 $f'(x)=0$ ，得 x=a，

①当 $a \leq 0$ 时， $f'(x) > 0$ 在 $(0, +\infty)$ 上恒成立， $\therefore f(x)$ 在 $(0, +\infty)$ 上单调递增，  
②当 $a > 0$ 时， $x \in (0, a)$ 时， $f'(x) < 0$ ， $x \in (a, +\infty)$ 时， $f'(x) > 0$ ，综上，当 $a \leq 0$ 时， $f(x)$ 在 $(0, +\infty)$ 上单调递增，当 $a > 0$ 时， $f(x)$ 在 $(0, a)$ 上单调递减，在 $(a, +\infty)$ 上单调递增.

# 【对点训练】

1. 已知函数 $f(x) = a\ln x - ax - 3 (a \in \mathbf{R})$ . 讨论函数 $f(x)$ 的单调性.

1. 解析 函数 $f(x)$ 的定义域为 $(0, +\infty)$ ，且 $f'(x)=\frac{a(1-x)}{x}$ ，令 $f'(x)=0$ ，得 x=1，

当 a>0 时， $f(x)$ 在 $(0,1)$ 上单调递增，在 $(1,+\infty)$ 上单调递减；

当 a<0 时， $f(x)$ 在 $(1, +\infty)$ 上单调递增，在 $(0, 1)$ 上单调递减；

当 a=0 时， $f(x)$ 为常函数.

2. 已知函数 $f(x)=\ln x-ax(a\in\mathbf{R})$ ，讨论函数 $f(x)$ 的单调性.

2. 解析 $f(x)$ 的定义域为 $(0, +\infty)$ ， $f'(x)=\frac{1}{x}-a=\frac{1-ax}{x}$ (x>0)，

①当 $a \leq 0$ 时， $f'(x) = \frac{1}{x} - a > 0$ ，即函数 $f(x)$ 在 $(0, +\infty)$ 上单调递增.

②当 a>0 时，令 $f(x)=\frac{1}{x}-a=\frac{1-ax}{x}=0$ ，可得 $x=\frac{1}{a}$ ,

当 $0 < x < \frac{1}{a}$ 时， $f(x) = \frac{1 - ax}{x} > 0$ ；当 $x > \frac{1}{a}$ 时， $f(x) = \frac{1 - ax}{x} < 0$ ，

故函数 $f(x)$ 在 $\left(0, \frac{1}{a}\right)$ 上单调递增，在 $\left(\frac{1}{a}, +\infty\right)$ 上单调递减.

综上，当 $a \leq 0$ 时， $f(x)$ 在 $(0, +\infty)$ 上单调递增；

当 a>0 时， $f(x)$ 在 $\left(0, \frac{1}{a}\right)$ 上单调递增，在 $\left(\frac{1}{a}, +\infty\right)$ 上单调递减.

# 考点二 导主二次型

# 【方法总结】

此类问题中，导数的解析式通过化简变形后，通常可以转化为一个二次函数的含参问题。对于二次三项式含参问题，有如下处理思路：

(1)首先需要考虑二次项系数是否含有参数．如果二次项系数有参数，就按二次项系数为零、为正、为负进行讨论；  
(2)其次考虑二次三项式能否因式分解，如果二次三项式能因式分解，这表明存在零点，只需讨论零点是否在定义域内，如果 $x_{1}$ ， $x_{2}$ 都在定义域内，则讨论个零点 $x_{1}$ ， $x_{2}$ 的大小；如果二次三项式不能因式分解，这表明不一定存在零点，需讨论判别式 $\Delta\leq0$ 和 $\Delta>0$ 分类讨论；

# 【例题选讲】

命题点 1 是不是 + 有没有 + 在不在

[例 2] (2021·全国乙节选)已知函数 $f(x)=x^{3}-x^{2}+ax+1$ 。讨论 $f(x)$ 的单调性.

解析 由题意知 $f(x)$ 的定义域为 R， $f'(x)=3x^{2}-2x+a$ ，对于 $f'(x)=0,\quad\Delta=(-2)^{2}-4\times3a=4(1-3a)$ .

①当 $a \geq \frac{1}{3}$ 时， $f'(x) \geq 0$ ， $f(x)$ 在 R 上单调递增；

②当 $a < \frac{1}{3}$ 时，令 $f(x) = 0$ ，即 $3x^{2} - 2x + a = 0$ ，解得 $x_{1} = \frac{1 - \sqrt{1 - 3a}}{3}, x_{2} = \frac{1 + \sqrt{1 - 3a}}{3},$

令 $f'(x)>0$ ，则 $x<x_{1}$ 或 $x>x_{2}$ ；令 $f'(x)<0$ ，则 $x_{1}<x<x_{2}$ .

所以 $f(x)$ 在 $(-∞, x_{1})$ 上单调递增，在 $(x_{1}, x_{2})$ 上单调递减，在 $(x_{2}, +∞)$ 上单调递增.

综上，当 $a \geq \frac{1}{3}$ 时， $f(x)$ 在 $\mathbf{R}$ 上单调递增；当 $a < \frac{1}{3}$ 时， $f(x)$ 在 $\left[-\infty, \frac{1 - \sqrt{1 - 3a}}{3}\right]$ 上单调递增，在 $\left[\frac{1 - \sqrt{1 - 3a}}{3}, \frac{1 + \sqrt{1 - 3a}}{3}\right]$ 上单调递减，在 $\left[\frac{1 + \sqrt{1 - 3a}}{3}, +\infty\right]$ 上单调递增.

[例 3] (2018·全国 I 节选) 已知函数 $f(x)=\frac{1}{x}-x+a\ln x$ ，讨论 $f(x)$ 的单调性.

解析 $f(x)$ 的定义域为 $(0, +\infty)$ ， $f'(x) = -\frac{1}{x^{2}} - 1 + \frac{a}{x} = -\frac{x^{2} - ax + 1}{x^{2}}$ .

①当 $a \leq 2$ 时，则 $f'(x) \leq 0$ ，当且仅当 a = 2，x = 1 时， $f'(x) = 0$ ，所以 $f(x)$ 在 $(0, +\infty)$ 上单调递减.

②当 a>2 时，令 $f(x)=0$ ，得 $x=\frac{a-\sqrt{a^{2}-4}}{2}$ 或 $x=\frac{a+\sqrt{a^{2}-4}}{2}$ .

当 $x \in \left(0, \frac{a - \sqrt{a^2 - 4}}{2}\right) \cup \left(\frac{a + \sqrt{a^2 - 4}}{2}, +\infty\right)$ 时， $f'(x) < 0$ ；当 $x \in \left(\frac{a - \sqrt{a^2 - 4}}{2}, \frac{a + \sqrt{a^2 - 4}}{2}\right)$ 时， $f'(x) > 0$ .

所以 $f(x)$ 在 $\left[0, \frac{a - \sqrt{a^2 - 4}}{2}\right], \left[\frac{a + \sqrt{a^2 - 4}}{2}, +\infty\right]$ 上单调递减，在 $\left[\frac{a - \sqrt{a^2 - 4}}{2}, \frac{a + \sqrt{a^2 - 4}}{2}\right]$ 上单调递增.

综合①②可知，当 $a \leq 2$ 时， $f(x)$ 在 $(0, +\infty)$ 上单调递减；当 $a > 2$ 时， $f(x)$ 在 $\left[0, \frac{a - \sqrt{a^2 - 4}}{2}\right]$ ，

$\left[\frac{a + \sqrt{a^2 - 4}}{2}, +\infty\right]$ 上单调递减，在 $\left[\frac{a - \sqrt{a^2 - 4}}{2}, \frac{a + \sqrt{a^2 - 4}}{2}\right]$ 上单调递增.

[例4] 设函数 $f(x) = a \ln x + \frac{x - 1}{x + 1}$ ，其中 $a$ 为常数。讨论函数 $f(x)$ 的单调性。

解析 函数 $f(x)$ 的定义域为 $(0, +\infty)$ . $f'(x) = \frac{a}{x} + \frac{2}{(x + 1)^2} = \frac{ax^2 + (2a + 2)x + a}{x(x + 1)^2}$ .

当 $a \geq 0$ 时， $f'(x) > 0$ ，函数 $f(x)$ 在 $(0, +\infty)$ 上单调递增.

当 a<0 时，令 $g(x)=ax^{2}+(2a+2)x+a$ ，由于 $\Delta=(2a+2)^{2}-4a^{2}=4(2a+1)$ .

(1) 当 $a = -\frac{1}{2}$ 时， $\Delta = 0$ ， $f(x) = \frac{-\frac{1}{2}(x - 1)^{2}}{x(x + 1)^{2}} \leq 0$ ，函数 $f(x)$ 在 $(0, +\infty)$ 上单调递减.  
(2) 当 $a < -\frac{1}{2}$ 时, $\Delta < 0$ , $g(x) < 0$ , $f'(x) < 0$ , 函数 $f(x)$ 在 $(0, +\infty)$ 上单调递减.  
(3)当 $-\frac{1}{2} < a < 0$ 时， $\Delta > 0$ 。设 $x_{1}, x_{2} (x_{1} < x_{2})$ 是函数 $g(x)$ 的两个零点，

则 $x_{1} = \frac{-(a + 1) + \sqrt{2a + 1}}{a}, x_{2} = \frac{-(a + 1) - \sqrt{2a + 1}}{a}$ .

由 $x_{1}=\frac{a+1-\sqrt{2a+1}}{-a}=\frac{\sqrt{a^{2}+2a+1}-\sqrt{2a+1}}{-a}>0$ ，所以

$x\in(0,\ x_{1})$ 时， $g(x)<0,\ f'(x)<0$ ，函数 $f(x)$ 单调递减；

$x\in(x_{1},x_{2})$ 时， $g(x)>0,\ f(x)>0$ ，函数 $f(x)$ 单调递增；

$x\in(x_{2},+\infty)$ 时， $g(x)<0,\ f'(x)<0$ ，函数 $f(x)$ 单调递减.

综上可得：当 $a \geq 0$ 时，函数 $f(x)$ 在 $(0, +\infty)$ 上单调递增；当 $a \leq -\frac{1}{2}$ 时，函数 $f(x)$ 在 $(0, +\infty)$ 上单调递减；当 $-\frac{1}{2} < a < 0$ 时， $f(x)$ 在 $\left[0, \frac{-(a+1)+\sqrt{2a+1}}{a}\right]$ ， $\left[\frac{-(a+1)-\sqrt{2a+1}}{a}, +\infty\right]$ 上单调递减，在 $\left[\frac{-(a+1)+\sqrt{2a+1}}{a}, \frac{-(a+1)-\sqrt{2a+1}}{a}\right]$ 上单调递增.

# 【对点训练】

3. (2020·全国III节选)已知函数 $f(x) = x^{3} - kx + k^{2}$ . 讨论 $f(x)$ 的单调性.  
3. 解析 由题意，得 $f'(x) = 3x^{2} - k$ ，

当 $k \leq 0$ 时， $f'(x) \geq 0$ 恒成立，所以 $f(x)$ 在 $(-∞, +∞)$ 上单调递增；

当 k>0 时，令 $f(x)=0$ ，得 $x=\pm\sqrt{\frac{k}{3}}$ ,

令 $f'(x) < 0$ ，得 $-\sqrt{\frac{k}{3}} < x < \sqrt{\frac{k}{3}}$ ，令 $f'(x) > 0$ ，得 $x < -\sqrt{\frac{k}{3}}$ 或 $x > \sqrt{\frac{k}{3}}$ ，

所以 $f(x)$ 在 $\left[-\sqrt{\frac{k}{3}}, \sqrt{\frac{k}{3}}\right]$ 上单调递减，在 $\left[-\infty, -\sqrt{\frac{k}{3}}\right], \left[\sqrt{\frac{k}{3}}, +\infty\right]$ 上单调递增.

4. 已知函数 $f(x) = x - \frac{2}{x} + 1 - a\ln x, a > 0$ . 讨论 $f(x)$ 的单调性.

4. 解析 由题意知， $f(x)$ 的定义域是 $(0, +\infty)$ ，导函数 $f'(x)=1+\frac{2}{x^{2}}-\frac{a}{x}=\frac{x^{2}-ax+2}{x^{2}}$ .

设 $g(x)=x^{2}-ax+2$ ，二次方程 $g(x)=0$ 的判别式 $\Delta=a^{2}-8$ .

①当 $\Delta<0$ ，即 $0<a<2\sqrt{2}$ 时，对一切x>0都有 $f'(x)>0$ 。此时 $f(x)$ 是 $(0,+\infty)$ 上的单调递增函数。

②当 $\Delta=0$ ，即 $a=2\sqrt{2}$ 时，仅对 $x=\sqrt{2}$ 有 $f(x)=0$ ，对其余的x>0都有 $f'(x)>0$ .

此时 $f(x)$ 是 $(0, +\infty)$ 上的单调递增函数.

③当 $\Delta>0$ ，即 $a>2\sqrt{2}$ 时，方程 $g(x)=0$ 有两个不同的实根 $x_{1}=\frac{a-\sqrt{a^{2}-8}}{2}$ ， $x_{2}=\frac{a+\sqrt{a^{2}-8}}{2}$ ， $0<x_{1}<x_{2}$

$x\in(0,\ x_{1})$ 时， $f(x)>0$ ，函数 $f(x)$ 单调递增；

$x \in (x_{1}, x_{2})$ 时， $f'(x) < 0$ ，函数 $f(x)$ 单调递减；

$x \in (x_{2}, +\infty)$ 时， $f'(x) > 0$ ，函数 $f(x)$ 单调递增.

此时 $f(x)$ 在 $\left\{0, \frac{a-\sqrt{a^{2}-8}}{2}\right\}$ 上单调递增，在 $\left(\frac{a-\sqrt{a^{2}-8}}{2}, \frac{a+\sqrt{a^{2}-8}}{2}\right)$ 上单调递减，在 $\left(\frac{a+\sqrt{a^{2}-8}}{2}, +\infty\right)$

上单调递增.

5. 已知函数 $f(x)=(1+ax^{2})\mathrm{e}^{x}-1$ ，当 $a\geq0$ 时，讨论函数 $f(x)$ 的单调性.

5. 解析 由题易得 $f'(x)=(ax^{2}+2ax+1)e^{x}$ ,

当 a=0 时， $f'(x)=\mathrm{e}^{x}>0$ ，此时 $f(x)$ 在 R 上单调递增.

当 a>0 时，方程 $ax^{2}+2ax+1=0$ 的判别式 $\Delta=4a^{2}-4a$ .

①当 $0 < a \leq 1$ 时， $\Delta \leq 0$ ， $ax^{2} + 2ax + 1 \geq 0$ 恒成立，所以 $f'(x) \geq 0$ ，此时 $f(x)$ 在 R 上单调递增；

②当 $a > 1$ 时，令 $f(x) = 0$ ，解得 $x_{1} = -1 - \sqrt{1 - \frac{1}{a}}$ ， $x_{2} = -1 + \sqrt{1 - \frac{1}{a}}.$

$x \in (-\infty, x_{1})$ 时， $f'(x) > 0$ ，函数 $f(x)$ 单调递增；

$x \in (x_{1}, x_{2})$ 时， $f'(x) < 0$ ，函数 $f(x)$ 单调递减；

$x \in (x_{2}, +\infty)$ 时， $f'(x) > 0$ ，函数 $f(x)$ 单调递增.

所以 $f(x)$ 在 $\left(-\infty, -1 - \sqrt{1 - \frac{1}{a}}\right)$ 和 $\left(-1 + \sqrt{1 - \frac{1}{a}}, +\infty\right)$ 上单调递增，

在 $\left(-1-\sqrt{1-\frac{1}{a}},-1+\sqrt{1-\frac{1}{a}}\right)$ 上单调递减.

综上，当 $0 \leq a \leq 1$ 时， $f(x)$ 在 $\mathbf{R}$ 上单调递增；当 $a > 1$ 时， $f(x)$ 在 $\left(-\infty, -1 - \sqrt{1 - \frac{1}{a}}\right)$ 和 $\left(-1 + \sqrt{1 - \frac{1}{a}}, +\infty\right)$

上单调递增，在 $\left[-1 - \sqrt{1 - \frac{1}{a}}, -1 + \sqrt{1 - \frac{1}{a}}\right]$ 上单调递减.

命题点 2 是不是 + 在不在 + 大不大

[例5] 已知函数 $f(x) = \ln x + ax^2 - (2a + 1)x$ . 若 $a > 0$ ，试讨论函数 $f(x)$ 的单调性.

解析 因为 $f(x)=\ln x+ax^{2}-(2a+1)x$ ，所以 $f'(x)=\frac{2ax^{2}-(2a+1)x+1}{x}=\frac{(2ax-1)(x-1)}{x}$ .

由题意知函数 $f(x)$ 的定义域为 $(0, +\infty)$ ，令 $f'(x)=0$ 得 x=1 或 $x=\frac{1}{2a}$ ,

若 $\frac{1}{2a}<1$ ，即 $a>\frac{1}{2}$ ，由 $f'(x)>0$ 得x>1或 $0<x<\frac{1}{2a}$ ，由 $f'(x)<0$ 得 $\frac{1}{2a}<x<1$ ，

即函数 $f(x)$ 在 $\left(0, \frac{1}{2a}\right)$ ， $(1, +\infty)$ 上单调递增，在 $\left(\frac{1}{2a}, 1\right)$ 上单调递减；

若 $\frac{1}{2a}>1$ ，即 $0<a<\frac{1}{2}$ ，由 $f'(x)>0$ 得 $x>\frac{1}{2a}$ 或0<x<1，由 $f'(x)<0$ 得 $1<x<\frac{1}{2a}$

即函数 $f(x)$ 在 $(0, 1)$ ， $\left(\frac{1}{2a}, +\infty\right)$ 上单调递增，在 $\left(1, \frac{1}{2a}\right)$ 上单调递减；

若 $\frac{1}{2a}=1$ ，即 $a=\frac{1}{2}$ ，则在 $(0,+\infty)$ 上恒有 $f'(x)\geq0$ ，即函数 $f(x)$ 在 $(0,+\infty)$ 上单调递增.

综上可得，当 $0 < a < \frac{1}{2}$ 时，函数 $f(x)$ 在 $(0, 1)$ 上单调递增，在 $\left[\frac{1}{2a}, \frac{1}{2a}\right]$ 上单调递减，在 $\left[\frac{1}{2a}, +\infty\right]$ 上单调递增；当 $a = \frac{1}{2}$ 时，函数 $f(x)$ 在 $(0, +\infty)$ 上单调递增；当 $a > \frac{1}{2}$ 时，函数 $f(x)$ 在 $\left[0, \frac{1}{2a}\right]$ 上单调递增，在 $\left[\frac{1}{2a}, 1\right]$ 上单调递减，在 $(1, +\infty)$ 上单调递增.

[例6] 已知函数 $f(x) = x^{2}\mathrm{e}^{-ax} - 1$ (a 是常数)，求函数 $y = f(x)$ 的单调区间.

解析 根据题意可得，当 $a = 0$ 时， $f(x) = x^2 - 1$ ，函数在 $(0, +\infty)$ 上单调递增，在 $(- \infty, 0)$ 上单调递减。当 $a \neq 0$ 时， $f'(x) = 2x\mathrm{e}^{-ax} + x^2(-a)\mathrm{e}^{-ax} = \mathrm{e}^{-ax}(-ax^2 + 2x)$ .

因为 $e^{-ax}>0$ ，所以令 $g(x)=-ax^{2}+2x=0$ ，解得 x=0 或 $x=\frac{2}{a}$ .

(1)当 $a > 0$ 时，函数 $g(x) = -ax^2 + 2x$ 在 $(- \infty, 0)$ 和 $\left[\frac{2}{a}, +\infty\right]$ 上有 $g(x) < 0$ ，即 $f'(x) < 0$ ，函数 $y = f(x)$ 单调递减；函数 $g(x) = -ax^2 + 2x$ 在 $\left[0, \frac{2}{a}\right]$ 上有 $g(x) \geq 0$ ，即 $f'(x) \geq 0$ ，函数 $y = f(x)$ 单调递增.

(2)当 $a < 0$ 时，函数 $g(x) = -ax^2 + 2x$ 在 $\left[-\infty, \frac{2}{a}\right]$ 和 $(0, +\infty)$ 上有 $g(x) > 0$ ，即 $f'(x) > 0$ ，函数 $y = f(x)$ 单调递增；函数 $g(x) = -ax^2 + 2x$ 在 $\left[\frac{2}{a}, 0\right]$ 上有 $g(x) \leq 0$ ，即 $f'(x) \leq 0$ ，函数 $y = f(x)$ 单调递减.

综上所述，当 a=0 时，函数 $y=f(x)$ 的单调递增区间为 $(0, +\infty)$ ，单调递减区间为 $(-∞, 0)$ ;

当 a>0 时，函数 $y=f(x)$ 的单调递减区间为 $(-∞, 0)$ ， $\left(\frac{2}{a}, +∞\right)$ ，单调递增区间为 $\left[0, \frac{2}{a}\right]$ ;

当 $a < 0$ 时，函数 $y = f(x)$ 的单调递增区间为 $\left[-\infty, \frac{2}{a}\right]$ ， $(0, +\infty)$ ，单调递减区间为 $\left[\frac{2}{a}, 0\right]$ .

[例 7] 已知函数 $f(x)=(a+1)\ln x+\frac{1}{x}-ax+2(a\in\mathbf{R})$ . 讨论 $f(x)$ 的单调性.

解析 $f(x)$ 的定义域为 $(0, +\infty)$ ，且 $f'(x) = -\frac{(x-1)(ax-1)}{x^2}$ 。令 $f'(x) = 0$ ，得 x = 1 或 $x = \frac{1}{a}$ .

当 $a \leq 0$ 时，ax - 1 < 0， $f(x)$ 在 $(0, 1)$ 上单调递减，在 $(1, +\infty)$ 上单调递增；

当 0 < a < 1 时， $f(x)$ 在 $(0, 1)$ 上单调递减，在 $\left(1, \frac{1}{a}\right)$ 上单调递增，在 $\left(\frac{1}{a}, +\infty\right)$ 上单调递减；

当 a=1 时， $f(x)$ 在 $(0, +\infty)$ 上单调递减；

当 $a > 1$ 时， $f(x)$ 在 $\left(0, \frac{1}{a}\right)$ 上单调递减，在 $\left(\frac{1}{a}, 1\right)$ 上单调递增，在 $(1, +\infty)$ 上单调递减.

[例 8] 已知函数 $f(x)=a\ln(x+1)-ax-x^{2}$ ，讨论 $f(x)$ 在定义域上的单调性.

解析 $f'(x)=\frac{a}{x+1}-a-2x=\frac{-2x\left(x+\frac{2+a}{2}\right)}{x+1}$ ,

令 $f'(x)=0$ ，得 x=0 或 $x=-\frac{a+2}{2}$ ，又 $f(x)$ 的定义域为 $(-1, +\infty)$ ,

①当 $-\frac{a+2}{2}\leq-1$ ，即当 $a\geq0$ 时，

若 $x \in (-1, 0)$ ， $f'(x) > 0$ ，则 $f(x)$ 单调递增；若 $x \in (0, +\infty)$ ， $f'(x) < 0$ ，则 $f(x)$ 单调递减。

②当 $-1<-\frac{a+2}{2}<0$ ，即-2<a<0时，

若 $x \in \left[-1, -\frac{a + 2}{2}\right]$ , $f(x) < 0$ , 则 $f(x)$ 单调递减; 若 $x \in \left[-\frac{a + 2}{2}, 0\right]$ , $f'(x) > 0$ , 则 $f(x)$ 单调递增; 若 $x \in (0, +\infty)$ , $f'(x) < 0$ , 则 $f(x)$ 单调递减.

③当 $-\frac{a+2}{2}=0$ ，即a=-2时， $f'(x)\leq0$ ， $f(x)$ 在 $(-1,+\infty)$ 上单调递减.

④当 $-\frac{a+2}{2}>0$ ，即a<-2时，若 $x\in(-1,0)$ ， $f'(x)<0$ ，则 $f(x)$ 单调递减；

若 $x \in \left(0, -\frac{a + 2}{2}\right)$ , $f(x) > 0$ , 则 $f(x)$ 单调递增; 若 $x \in \left(-\frac{a + 2}{2}, +\infty\right)$ , $f'(x) < 0$ , 则 $f(x)$ 单调递减.

综上，当 $a \geq 0$ 时， $f(x)$ 在 $(-1, 0)$ 上单调递增，在 $(0, +\infty)$ 上单调递减；当 $-2 < a < 0$ 时， $f(x)$ 在 $\left[-1, -\frac{a+2}{2}\right]$ 上单调递减，在 $\left[-\frac{a+2}{2}, 0\right]$ 上单调递增，在 $(0, +\infty)$ 上单调递减；当 $a = -2$ 时， $f(x)$ 在 $(-1, +\infty)$ 上单调递减；当 $a < -2$ 时， $f(x)$ 在 $(-1, 0)$ 上单调递减，在 $\left[0, -\frac{a+2}{2}\right]$ 上单调递增，在 $\left[-\frac{a+2}{2}, +\infty\right]$ 上单调递减。

[例 9] (2016·山东)已知 $f(x)=a(x-\ln x)+\frac{2x-1}{x^{2}}$ ， $a\in R$ 。讨论 $f(x)$ 的单调性.

解析 $f(x)$ 的定义域为 $(0, +\infty)$ ， $f'(x)=a-\frac{a}{x}-\frac{2}{x^{2}}+\frac{2}{x^{3}}=\frac{(ax^{2}-2)(x-1)}{x^{3}}$ .

当 $a \leq 0,\quad x \in (0,1)$ 时， $f'(x) > 0,\quad f(x)$ 单调递增； $x \in (1,+\infty)$ 时， $f'(x) < 0,\quad f(x)$ 单调递减.

当 a>0 时， $f'(x)=\frac{a(x-1)}{x^{3}}\left[x-\sqrt{\frac{2}{a}}\right]\left[x+\sqrt{\frac{2}{a}}\right]$ .

①若 0 < a < 2，则 $\sqrt{\frac{2}{a}} > 1$ ，当 $x \in (0, 1)$ 或 $x \in \left(\sqrt{\frac{2}{a}}, +\infty\right)$ 时， $f'(x) > 0$ ， $f(x)$ 单调递增；当 $x \in \left(1, \sqrt{\frac{2}{a}}\right)$ 时， $f'(x) < 0$ ， $f(x)$ 单调递减.

②若 a=2，则 $\sqrt{\frac{2}{a}}=1$ ，在 $x\in(0,+\infty)$ 内， $f'(x)\geq0$ ， $f(x)$ 单调递增.

③若 a>2，则 $0<\sqrt{\frac{2}{a}}<1$ ，当 $x\in\left(0,\sqrt{\frac{2}{a}}\right)$ 或 $x\in(1,+\infty)$ 时， $f'(x)>0,f(x)$ 单调递增；当 $x\in\left(\sqrt{\frac{2}{a}},1\right)$ 时， $f'(x)<0,f(x)$ 单调递减.

综上所述，当 $a \leq 0$ 时， $f(x)$ 在 $(0, 1)$ 内单调递增，在 $(1, +\infty)$ 内单调递减；当 $0 < a < 2$ 时， $f(x)$ 在 $(0, 1)$ 内单调递增，在 $\left[1, \sqrt{\frac{2}{a}}\right]$ 内单调递减，在 $\left[\sqrt{\frac{2}{a}}, +\infty\right]$ 内单调递增；当 $a = 2$ 时， $f(x)$ 在 $(0, +\infty)$ 内单调递增；当 $a > 2$ 时， $f(x)$ 在 $\left[0, \sqrt{\frac{2}{a}}\right]$ 内单调递增，在 $\left[\sqrt{\frac{2}{a}}, 1\right]$ 内单调递减，在 $(1, +\infty)$ 内单调递增.

# 【对点训练】

6. 已知函数 $f(x) = \frac{1}{2} ax^2 - (a + 1)x + \ln x, a > 0$ ，试讨论函数 $y = f(x)$ 的单调性.

6. 解析 函数的定义域为 $(0, +\infty)$ ,

$$
f ^ {\prime} (x) = a x - (a + 1) + \frac {1}{x} = \frac {a x ^ {2} - (a + 1) x + 1}{x} = \frac {(a x - 1) (x - 1)}{x}.
$$

①当 0<a<1 时， $\frac{1}{a}>1,\quad\therefore x\in(0,1)$ 和 $\left(\frac{1}{a},+\infty\right)$ 时， $f'(x)>0;\quad x\in\left(1,\frac{1}{a}\right)$ 时， $f'(x)<0,$

∴函数 $f(x)$ 在 $(0, 1)$ 和 $\left(\frac{1}{a}, +\infty\right)$ 上单调递增，在 $\left(1, \frac{1}{a}\right)$ 上单调递减；

②当 a=1 时， $\frac{1}{a}=1,\quad\therefore f'(x)\geq0$ 在 $(0,+\infty)$ 上恒成立，∴函数 $f(x)$ 在 $(0,+\infty)$ 上单调递增；

③当 a>1 时， $0<\frac{1}{a}<1,\quad\therefore x\in\left(0,\quad\frac{1}{a}\right)$ 和 $(1,+\infty)$ 时， $f'(x)>0;\quad x\in\left(\frac{1}{a},\quad1\right)$ 时， $f'(x)<0,$

∴函数 $f(x)$ 在 $\left(0, \frac{1}{a}\right)$ 和 $(1, +\infty)$ 上单调递增，在 $\left(\frac{1}{a}, 1\right)$ 上单调递减.

综上，当 0<a<1 时，函数 $f(x)$ 在 $(0,1)$ 和 $\left(\frac{1}{a},+\infty\right)$ 上单调递增，在 $\left(1,\frac{1}{a}\right)$ 上单调递减；当 a=1 时，函数 $f(x)$ 在 $(0,+\infty)$ 上单调递增；

当 a>1 时，函数 $f(x)$ 在 $\left(0, \frac{1}{a}\right)$ 和 $(1, +\infty)$ 上单调递增，在 $\left(\frac{1}{a}, 1\right)$ 上单调递减.

7. 已知函数 $f(x) = x^{2}\mathrm{e}^{ax + 1} + 1 - a(a\in \mathbf{R})$ ，求函数 $f(x)$ 的单调区间.

7. 解析 $f(x)=x^{2}\mathrm{e}^{ax+1}+1-a(a\in\mathbf{R})$ 的定义域为 $(-∞,+∞)$ ， $f'(x)=x(ax+2)\mathrm{e}^{ax+1}$

①当 a=0 时，x>0， $f'(x)>0$ ; x<0， $f'(x)<0$ ,

所以函数 $f(x)$ 的单调递增区间为 $(0, +\infty)$ ，单调递减区间为 $(-∞, 0)$ .

②当 a>0 时， $x\in\left(-\infty,-\frac{2}{a}\right)$ ， $f(x)>0;\quad x\in\left(-\frac{2}{a},0\right)$ ， $f'(x)<0;\quad x\in(0,+\infty)$ ， $f'(x)>0$

所以函数 $f(x)$ 的单调递增区间为 $\left(-\infty, -\frac{2}{a}\right), (0, +\infty)$ ，单调递减区间为 $\left(-\frac{2}{a}, 0\right)$ .

③当 a<0 时， $x\in(-\infty,0)$ ， $f(x)<0;\quad x\in\left(0,-\frac{2}{a}\right)$ ， $f(x)>0;\quad x\in\left(-\frac{2}{a},+\infty\right)$ ， $f(x)<0$

所以函数 $f(x)$ 的单调递减区间为 $(-∞, 0)$ ， $\left(-\frac{2}{a}, +∞\right)$ ，单调递增区间为 $\left(0, -\frac{2}{a}\right)$ .

8. 已知函数 $f(x)=(a-1)\ln x+ax^{2}+1$ ，讨论函数 $f(x)$ 的单调性.

8. 解析 $f(x)$ 的定义域为 $(0, +\infty)$ ， $f'(x)=\frac{a-1}{x}+2ax=\frac{2ax^{2}+a-1}{x}$ .

(1) 当 $a \geq 1$ 时， $f'(x) > 0$ ，故 $f(x)$ 在 $(0, +\infty)$ 上单调递增；  
(2) 当 $a \leq 0$ 时， $f'(x) < 0$ ，故 $f(x)$ 在 $(0, +\infty)$ 上单调递减；  
(3)当 $0 < a < 1$ 时，令 $f'(x) = 0$ ，解得 $x = \sqrt{\frac{1 - a}{2a}}$

则当 $x \in \left[0, \sqrt{\frac{1 - a}{2a}}\right]$ 时， $f'(x) < 0$ ；当 $x \in (\sqrt{\frac{1 - a}{2a}}, +\infty)$ 时， $f'(x) > 0$

故 $f(x)$ 在 $\left(0, \sqrt{\frac{1-a}{2a}}\right)$ 上单调递减，在 $(\sqrt{\frac{1-a}{2a}}, +\infty)$ 上单调递增.

9. 已知函数 $f(x) = \left(k + \frac{4}{k}\right)\ln x + \frac{4 - x^2}{x}$ ，其中常数 $k > 0$ ，讨论 $f(x)$ 在(0, 2)上的单调性.  
9. 解 因为 $f'(x)=\frac{k+\frac{4}{k}}{x}-\frac{4}{x^{2}}-1=\frac{\left(k+\frac{4}{k}\right)x-4-x^{2}}{x^{2}}=-\frac{(x-k)\left(x-\frac{4}{k}\right)}{x^{2}}(x>0,\ k>0)$ .

①当 0<k<2 时， $\frac{4}{k}>k>0$ ，且 $\frac{4}{k}>2$ ，所以当 $x\in(0,k)$ 时， $f'(x)<0$ ，当 $x\in(k,2)$ 时， $f'(x)>0$ ，

所以函数 $f(x)$ 在 $(0, k)$ 上是减函数，在 $(k, 2)$ 上是增函数；

②当 k=2 时， $\frac{4}{k}=k=2,\ f'(x)<0$ 在 $(0,2)$ 上恒成立，所以 $f(x)$ 在 $(0,2)$ 上是减函数；  
③当 k>2 时， $0<\frac{4}{k}<2,\quad k>\frac{4}{k}$ ，所以当 $x\in\left(0,\quad\frac{4}{k}\right)$ 时， $f'(x)<0$ ；当 $x\in\left(\frac{4}{k},\quad2\right)$ 时， $f'(x)>0$ ，

所以函数 $f(x)$ 在 $\left(0, \frac{4}{k}\right)$ 上是减函数，在 $\left(\frac{4}{k}, 2\right)$ 上是增函数.

综上可知，当 0<k<2 时， $f(x)$ 在 $(0, k)$ 上是减函数，在 $(k, 2)$ 上是增函数；当 k=2 时， $f(x)$ 在 $(0, 2)$ 上是减函数；当 k>2 时， $f(x)$ 在 $\left(0, \frac{4}{k}\right)$ 上是减函数，在 $\left(\frac{4}{k}, 2\right)$ 上是增函数.

10. 已知函数 $f(x) = \ln (x + 1) - \frac{ax^2 + x}{(x + 1)^2}$ ，且 $1 < a < 2$ ，试讨论函数 $f(x)$ 的单调性.  
10. 解析 函数 $f(x)$ 的定义域为 $(-1, +\infty)$ ， $f'(x)=\frac{x(x-2a+3)}{(x+1)^{3}}$ ，x>-1.

①当-1<2a-3<0，即 $1<a<\frac{3}{2}$ 时，

当-1<x<2a-3或x>0时， $f'(x)>0$ ， $f(x)$ 单调递增，当2a-3<x<0时， $f'(x)<0$ ， $f(x)$ 单调递减.

②当 2a-3=0，即 $a=\frac{3}{2}$ 时， $f'(x)\geq0$ ，则 $f(x)$ 在 $(-1,+\infty)$ 上单调递增.

③当 2a-3>0，即 $\frac{3}{2}<a<2$ 时，

当-1<x<0 或 x>2a-3 时， $f'(x)>0$ ，则 $f(x)$ 在 $(-1,0)$ ， $(2a-3,+\infty)$ 上单调递增.

当 0<x<2a-3 时， $f(x)<0$ ，则 $f(x)$ 在 $(0, 2a-3)$ 上单调递减.

综上，当 $1 < a < \frac{3}{2}$ 时， $f(x)$ 在 $(-1, 2a - 3)$ ， $(0, +\infty)$ 上单调递增，在 $(2a - 3, 0)$ 上单调递减；当 $a = \frac{3}{2}$ 时， $f(x)$ 在 $(-1, +\infty)$ 上单调递增；当 $\frac{3}{2} < a < 2$ 时， $f(x)$ 在 $(-1, 0)$ ， $(2a - 3, +\infty)$ 上单调递增，在 $(0, 2a - 3)$ 上单调递减。

# 考点三 导主指对型

# 【例题选讲】

[例 10] 已知函数 $f(x)=\mathrm{e}^{x}(\mathrm{e}^{x}-a)-a^{2}x$ ，讨论函数 $f(x)$ 的单调性.

解析 函数 $f(x)$ 的定义域为 $(-∞, +∞)$ ， $f'(x)=2e^{2x}-ae^{x}-a^{2}=(2e^{x}+a)(e^{x}-a)$ .

①若 a=0，则 $f(x)=\mathrm{e}^{2x}$ 在 $(-∞,+∞)$ 上单调递增.

②若 a>0，则由 $f'(x)=0$ ，得 $x=\ln a$ 。当 $x\in(-\infty,\ \ln a)$ 时， $f'(x)<0$ ；当 $x\in(\ln a,+\infty)$ 时， $f'(x)>0$ 。

故 $f(x)$ 在 $(-∞, ∫a)$ 上单调递减，在 $(\ln a, +∞)$ 上单调递增.

③若 $a < 0$ ，则由 $f(x) = 0$ ，得 $x = \ln \left(-\frac{a}{2}\right)$ 。当 $x \in \left(-\infty, \ln \left(-\frac{a}{2}\right)\right)$ 时， $f'(x) < 0$ ；当 $x \in \left(\ln \left(-\frac{a}{2}\right), +\infty\right)$ 时， $f'(x) > 0$ ；故 $f(x)$ 在 $\left(-\infty, \ln \left(-\frac{a}{2}\right)\right)$ 上单调递减，在 $\left(\ln \left(-\frac{a}{2}\right), +\infty\right)$ 上单调递增。

[例11] 已知 $f(x) = (x^2 - ax)\ln x - \frac{3}{2} x^2 + 2ax$ ，求 $f(x)$ 的单调递减区间.

解析 易得 $f(x)$ 的定义域为 $(0, +\infty)$ ,

$$
f (x) = (2 x - a) \ln x + x - a - 3 x + 2 a = (2 x - a) \ln x - (2 x - a) = (2 x - a) (\ln x - 1),
$$

令 $f'(x)=0$ 得 $x=\frac{a}{2}$ 或 x=e.

当 $a \leq 0$ 时，因为 x > 0，所以 2x - a > 0，令 $f'(x) < 0$ 得 x < e，所以 $f(x)$ 的单调递减区间为 $(0, e)$ . 当 a > 0 时，

①若 $\frac{a}{2}<e$ ，即0<a<2e，

当 $x \in \left(0, \frac{a}{2}\right)$ 时， $f'(x) > 0$ ，当 $x \in \left(\frac{a}{2}, e\right)$ 时， $f'(x) < 0$ ，当 $x \in (e, +\infty)$ 时， $f'(x) > 0$ ，
所以 $f(x)$ 的单调递减区间为 $\left(\frac{a}{2}, e\right)$ ;

②若 $\frac{a}{2}=e$ ，即a=2e，当 $x\in(0,+\infty)$ 时， $f'(x)\geq0$ 恒成立， $f(x)$ 没有单调递减区间；  
③若 $\frac{a}{2}>e$ ，即a>2e，当 $x\in(0,e)$ 时，

$$
f ^ {\prime} (x) > 0, \text {当} x \in \left[ \mathrm{e}, \frac {a}{2} \right] \text {时,} f ^ {\prime} (x) <   0, \text {当} x \in \left[ \frac {a}{2}, + \infty \right] \text {时,} f ^ {\prime} (x) > 0,
$$

所以 $f(x)$ 的单调递减区间为 $\left(\mathrm{e}, \frac{a}{2}\right)$ .

综上所述，当 $a \leq 0$ 时， $f(x)$ 的单调递减区间为 $(0, e)$ ；当 $0 < a < 2e$ 时， $f(x)$ 的单调递减区间为 $\left[\frac{a}{2}, e\right]$ ；当 $a = 2e$ 时， $f(x)$ 无单调递减区间；当 $a > 2e$ 时， $f(x)$ 的单调递减区间为 $\left[\mathrm{e}, \frac{a}{2}\right]$ .

# 【对点训练】

11. 已知函数 $f(x) = \mathrm{e}^{x} - ax - 1$ 的定义域为 $(0, +\infty)$ ，讨论函数 $f(x)$ 的单调性.  
11. 解析 $\because f(x)=\mathrm{e}^{x}-ax-1,\quad\therefore f'(x)=\mathrm{e}^{x}-a.$ 易知 $f'(x)=\mathrm{e}^{x}-a$ 在 $(0,+\infty)$ 上单调递增.

∴当 $a \leq 1$ 时， $f'(x) > 0$ ，故 $f(x)$ 在 $(0, +\infty)$ 上单调递增；

当 a>1 时，由 $f'(x)=\mathrm{e}^{x}-a=0$ ，得 $x=\ln a$ ，∴当 $0<x<\ln a$ 时， $f'(x)<0$ ，当 $x>\ln a$ 时， $f'(x)>0$ ，

∴ $f(x)$ 在 $(0, \ln a)$ 上单调递减，在 $(\ln a, +\infty)$ 上单调递增.

综上，当 $a \leq 1$ 时， $f(x)$ 在 $(0, +\infty)$ 上单调递增；当 a > 1 时， $f(x)$ 在 $(0, \ln a)$ 上单调递减，在 $(\ln a, +\infty)$ 上单调递增.

12. 已知函数 $f(x) = (x^2 - 2ax)\ln x - \frac{1}{2} x^2 + 2ax (a \in \mathbf{R})$ .

(1)若 a=0，求 $f(x)$ 的最小值；  
(2)求函数 $f(x)$ 的单调区间.

12. 解析 (1) 若 $a=0$ , $f(x)=x^{2}\ln x-\frac{1}{2}x^{2}$ , 定义域为 $(0,+\infty)$ , $f'(x)=2x\ln x+x^{2}\times\frac{1}{x}-x=2x\ln x$ ,

由 $f'(x)>0$ 可得 x>1，由 $f'(x)<0$ 可得 0<x<1，

所以 $f(x)$ 在 $(0, 1)$ 单调递减，在 $(1, +\infty)$ 单调递增，所以 $f(x)$ 的最小值为 $f(1) = -\frac{1}{2}$ .

$$
(2) f ^ {\prime} (x) = (2 x - 2 a) \ln x + \left(x ^ {2} - 2 a x\right) \cdot \frac {1}{x} - x + 2 a = (2 x - 2 a) \ln x,
$$

①当 $a \leq 0$ 时，2x - 2a > 0，由 $f'(x) > 0$ 可得 x > 1，由 $f'(x) < 0$ 可得 0 < x < 1，

此时 $f(x)$ 的单调递减区间为 $(0, 1)$ ，单调递增区间为 $(1, +\infty)$ ;

②当 0 < a < 1 时，由 $f'(x) > 0$ 可得 0 < x < a 或 x > 1，由 $f'(x) < 0$ 可得 a < x < 1，

此时 $f(x)$ 的单调递减区间为 $(a, 1)$ ，单调递增区间为 $(0, a)$ 和 $(1, +\infty)$ ;

③当 a=1 时， $f(x)\geq0$ 恒成立，此时 $f(x)$ 的单调递增区间为 $(0,+\infty)$ ;

④当 a>1 时，由 $f'(x)>0$ 可得 0<x<1 或 x>a，由 $f'(x)<0$ 可得 1<x<a，

此时 $f(x)$ 的单调递减区间为 $(1, a)$ ，单调递增区间为 $(0, 1)$ 和 $(a, +\infty)$ .

综上所述：当 $a \leq 0$ 时， $f(x)$ 的单调递减区间为 $(0, 1)$ ，单调递增区间为 $(1, +\infty)$ ;

当 0 < a < 1 时， $f(x)$ 的单调递减区间为 $(a, 1)$ ，单调递增区间为 $(0, a)$ 和 $(1, +\infty)$ ;

当 a=1 时， $f(x)$ 的单调递增区间为 $(0, +\infty)$ ，无单调递减区间；

当 a>1 时， $f(x)$ 的单调递减区间为 $(1, a)$ ，单调递增区间为 $(0, 1)$ 和 $(a, +\infty)$ .

# 考点四 导主正余型

# 【例题选讲】

[例 12] (2017 山东理) 已知函数 $f(x)=x^{2}+2\cos x$ ， $g(x)=\mathrm{e}^{x}\cdot(\cos x-\sin x+2x-2)$ ，其中 e 是自然对数的底数.

(1)求函数 $g(x)$ 的单调区间;

(2)讨论函数 $h(x)=g(x)-af(x)(a\in\mathbf{R})$ 的单调性.

解析 (1) $g'(x)=(e^{x})' \cdot (\cos x-\sin x+2x-2)+e^{x}(\cos x-\sin x+2x-2)'$

$$
= \mathrm{e} ^ {x} (\cos x - \sin x + 2 x - 2 - \sin x - \cos x + 2) = 2 \mathrm{e} ^ {x} (x - \sin x).
$$

记 $p(x)=x-\sin x$ ，则 $p'(x)=1-\cos x$ .

因为 $\cos x \in [-1, 1]$ ，所以 $p'(x) = 1 - \cos x \geq 0$ ，所以函数 $p(x)$ 在 R 上单调递增.

而 $p(0)=0-\sin 0=0$ ，所以当 x<0 时， $p(x)<0,\quad g'(x)<0$ ，函数 $g(x)$ 单调递减；

当 x>0 时， $p(x)>0$ ， $g'(x)>0$ ，函数 $g(x)$ 单调递增.

综上，函数 $g(x)$ 的单调递减区间为 $(-∞, 0)$ ，单调递增区间为 $(0, +∞)$ .

(2) 因为 $h(x)=g(x)-af(x)=\mathrm{e}^{x}(\cos x-\sin x+2x-2)-a(x^{2}+2\cos x)$ ,

所以 $h'(x)=2\mathrm{e}^{x}(x-\sin x)-a(2x-2\sin x)=2(x-\sin x)(\mathrm{e}^{x}-a)$ .

由(1)知，当 x>0 时， $p(x)=x-\sin x>0$ ；当 x<0 时， $p(x)=x-\sin x<0$ .

当 $a \leq 0$ 时， $e^{x} - a > 0$ ，所以 x > 0 时， $h'(x) > 0$ ，函数 $h(x)$ 单调递增；x < 0 时， $h'(x) < 0$ ，函数 $h(x)$ 单调递减。

当 a>0 时，令 $h'(x)=2(x-\sin x)(e^{x}-a)=0$ ，解得 $x_{1}=\ln a,\quad x_{2}=0$ .

①若 0<a<1，则 $\ln a<0$ ，所以 $x\in(-\infty,\ \ln a)$ 时， $e^{x}-a<0,\ h'(x)>0$ ，函数 $h(x)$ 单调递增；

$x\in(\ln a,0)$ 时， $e^{x}-a>0,\ h'(x)<0$ ，函数 $h(x)$ 单调递减；

$x\in(0,+\infty)$ 时， $e^{x}-a>0,\ h'(x)>0$ ，函数 $h(x)$ 单调递增.

②若 a=1，则 $\ln a=0$ ，所以 $x\in R$ 时， $h'(x)\geq0$ ，函数 $h(x)$ 在 R 上单调递增.

③若 a>1，则 $\ln a>0$ ，所以 $x\in(-\infty,0)$ 时， $e^{x}-a<0,\ h'(x)>0$ ，函数 $h(x)$ 单调递增；

$x \in (0, \ln a)$ 时， $e^{x} - a < 0$ ， $h'(x) < 0$ ，函数 $h(x)$ 单调递减；

$x\in(\ln a,+\infty)$ 时， $e^{x}-a>0,\ h'(x)>0$ ，函数 $h(x)$ 单调递增.

综上所述，当 $a \leq 0$ 时，函数 $h(x)$ 在 $(0, +\infty)$ 上单调递增，在 $(-∞, 0)$ 上单调递减；

当 0<a<1 时，函数 $h(x)$ 在 $(-∞, �ln a)$ ， $(0, +∞)$ 上单调递增，在 $(\ln a, 0)$ 上单调递减；

当 a=1 时，函数 $h(x)$ 在 R 上单调递增；

当 a>1 时，函数 $h(x)$ 在 $(-∞, 0)$ ， $(\ln a, +∞)$ 上单调递增，在 $(0, \ln a)$ 上单调递减.

# 【对点训练】

13. (2017·山东)已知函数 $f(x)=\frac{1}{3}x^{3}-\frac{1}{2}ax^{2}$ ，其中参数 $a \in R$ .

(1)当 a=2 时，求曲线 $y=f(x)$ 在点 $(3, f(3))$ 处的切线方程；  
(2)设函数 $g(x)=f(x)+(x-a)\cos x-\sin x$ ，讨论 $g(x)$ 的单调性.

13. 解析 (1)由题意得 $f'(x)=x^{2}-ax$ ，所以当 a=2 时， $f(3)=0$ ， $f'(x)=x^{2}-2x$ ，所以 $f'(3)=3$ ，

因此曲线 $y=f(x)$ 在点 $(3, f(3))$ 处的切线方程是 $y=3(x-3)$ ，即 3x-y-9=0.

(2) 因为 $g(x)=f(x)+(x-a)\cos x-\sin x$ ,

所以 $g'(x)=f'(x)+\cos x-(x-a)\sin x-\cos x=x(x-a)-(x-a)\sin x=(x-a)(x-\sin x)$ .

令 $h(x)=x-\sin x$ ，则 $h'(x)=1-\cos x\geq0$ ，所以 $h(x)$ 在 R 上单调递增.

因为 $h(0)=0$ ，所以当 x>0 时， $h(x)>0$ ；当 x<0 时， $h(x)<0$ 。

①当 a<0 时， $g'(x)=(x-a)(x-\sin x)$ ,

当 $x \in (-\infty, a)$ 时，x - a < 0， $g'(x) > 0$ ， $g(x)$ 单调递增；当 $x \in (a, 0)$ 时，x - a > 0， $g'(x) < 0$ ， $g(x)$ 单调递减；当 $x \in (0, +\infty)$ 时，x - a > 0， $g'(x) > 0$ ， $g(x)$ 单调递增.

②当 a=0 时， $g'(x)=x(x-\sin x)$ ，当 $x\in(-\infty,+\infty)$ 时， $g'(x)\geq0$ ，所以 $g(x)$ 在 $(-∞,+∞)$ 上单调递增.  
③当 a>0 时， $g'(x)=(x-a)(x-\sin x)$ ,

当 $x \in (-\infty, 0)$ 时，x - a < 0， $g'(x) > 0$ ， $g(x)$ 单调递增；当 $x \in (0, a)$ 时，x - a < 0， $g'(x) < 0$ ， $g(x)$ 单调递减；当 $x \in (a, +\infty)$ 时，x - a > 0， $g'(x) > 0$ ， $g(x)$ 单调递增.

综上所述，当 a<0 时，函数 $g(x)$ 在 $(-∞, a)$ 和 $(0, +∞)$ 上单调递增，在 $(a, 0)$ 上单调递减；当 a=0 时，函数 $g(x)$ 在 $(-∞, +∞)$ 上单调递增；当 a>0 时，函数 $g(x)$ 在 $(-∞, 0)$ 和 $(a, +∞)$ 上单调递增，在 $(0, a)$ 上单调递减.

