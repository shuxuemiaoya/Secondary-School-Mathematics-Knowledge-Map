![](019188542282c51bbbb860d7fb3e0097.png)第05讲 5拓展一：利用导数研究不等式恒成立问题
### 一、知识点归纳
1、分离参数法
用分离参数法解含参不等式恒成立问题，可以根据不等式的性质将参数分离出来，得到一个一端是参数，另一端是变量表达式的不等式；
步骤：
①分类参数（注意分类参数时自变量$x$的取值范围是否影响不等式的方向）
②转化：若$a > f ( x )$)对$x \in D$恒成立，则只需$a > f ( x )_{\max}$；若$a < f ( x )$对$x \in D$恒成立，则只需$a < f ( x )_{\min}$．
③求最值.
2、分类讨论法
如果无法分离参数，可以考虑对参数或自变量进行分类讨论求解，如果是二次不等式恒成立的问题，可以考虑二次项系数与判别式的方法($a > 0$，$\Delta < 0$或$a < 0$，$\Delta < 0$)求解．
3、等价转化法
当遇到$f ( x ) \geq g ( x )$型的不等式恒成立问题时，一般采用作差法，构造“左减右”的函数$F ( x ) = f ( x ) - g ( x )$或者“右减左”的函数$H ( x ) = g ( x ) - f ( x )$，进而只需满足$F ( x )_{\min}$，或者$H ( x )_{\max}$，将比较法的思想融入函数中，转化为求解函数的最值的问题.
### 二、题型精讲
方法一：分离变量法
##### 1．（2023上·北京通州·高三统考期中）已知函数$f ( x ) = e^{2 x} - 2 x$.
(1)求曲线在点$\left( 0 , f ( 0 ) \right)$处的切线方程；
(2)求$f ( x )$的极值；
(3)若对于任意$x \in \mathbf{R}$，不等式$f ( x ) > 2 ( e - 1 ) x + m$恒成立，求实数$m$的取值范围.
<span class="fake-tag">答案</span>(1)$y = 1$
(2)极小值为$f ( 0 ) = 1$，无极大值
(3)$m < 0$
<span class="fake-tag">详解</span>（1）由$f ( x ) = e^{2 x} - 2 x$得$f^{'} ( x ) = 2 e^{2 x} - 2$，又$f^{'} ( 0 ) = 0 , f ( 0 ) = 1$，
所以在切线为$y = 1$
（2）令$f^{'} ( x ) = 2 e^{2 x} - 2 > 0$，则$x > 0$，故$f ( x )$在$( 0 , + \infty )$单调递增，
当$x < 0$时，$f^{'} ( x ) < 0 , f ( x )$单调递减，
所以当$x = 0$时，$f ( x )$取极小值$f ( 0 ) = 1$，无极大值，
（3）由$f ( x ) > 2 ( e - 1 ) x + m$得$e^{2 x} - 2 x > 2 ( e - 1 ) x + m$，
故$e^{2 x} - 2 e x > m$，
构造函数$g ( x ) = e^{2 x} - 2 e x ,$则$g^{'} ( x ) = 2 e^{2 x} - 2 e$，令$g^{'} ( x ) = 2 e^{2 x} - 2 \text{e>0}$，则$x > \frac{1}{2}$，
故当$x > \frac{1}{2}$时，$g^{'} ( x ) > 0$，$g ( x )$单调递增，$x < \frac{1}{2}$时，$g^{'} ( x ) < 0 , g ( x )$单调递减，
故当$x = \frac{1}{2} , g ( x )$取极小值也是最小值，$g \left( \frac{1}{2} \right) = e - e = 0$，
所以$m < g ( x )_{\min}$，即$m < 0$
##### 2．（2023下·山东淄博·高二校考阶段练习）（1）已知$a \leq \frac{1 - x}{x} + \text{ln} x$对于$x \in \left\lbrack \frac{1}{2} , 2 \right\rbrack$恒成立，求实数$a$的取值范围；
（2）已知函数$f ( x ) = a e^{x} - 2 x^{2}$，若不等式$f^{'} ( x ) \geq 0$在R上恒成立，试求*a*的取值范围.
<span class="fake-tag">答案</span>（1）$\left. - \infty , 0 \right.$；（2）$\left. \frac{4}{e} , + \infty \right)$
<span class="fake-tag">详解</span>（1）$a \leq \frac{1 - x}{x} + \text{ln} x$对于$x \in \left\lbrack \frac{1}{2} , 2 \right\rbrack$恒成立，
令$g ( x ) = \frac{1 - x}{x} + \text{ln} x$，$x \in \left\lbrack \frac{1}{2} , 2 \right\rbrack$，
只需$a \leq g ( x )_{\min}$即可，
则$g^{'} ( x ) = \frac{- x - ( 1 - x )}{x^{2}} + \frac{1}{x} = \frac{x - 1}{x^{2}}$，
当$x \in \left. 1 , 2 \right.$时，$g^{'} ( x ) > 0$，当$x \in \left. \frac{1}{2} , 1 \right)$时，$g^{'} ( x ) < 0$，
所以$g ( x ) = \frac{1 - x}{x} + \text{ln} x$在$x \in \left. 1 , 2 \right.$上单调递增，在$x \in \left. \frac{1}{2} , 1 \right)$上单调递减，
所以当$x = 1$时，$g ( x ) = \frac{1 - x}{x} + \text{ln} x$取得极小值，也是最小值，
所以$g {( x ) ( 1 )}_{\min}$，
故$a \leq 0$，
实数$a$的取值范围是$\left. - \infty , 0 \right.$；
（2）$f^{'} ( x ) = a e^{x} - 4 x$，故$a e^{x} - 4 x \geq 0$在R上恒成立，
即$a \geq \frac{4 x}{e^{x}}$在R上恒成立，
令$h ( x ) = \frac{4 x}{e^{x}}$，
只需$a \geq h ( x )_{\max}$，
则$h^{'} ( x ) = \frac{4 - 4 x}{e^{x}}$，
当$x < 1$时，$h^{'} ( x ) > 0$，$h ( x ) = \frac{4 x}{e^{x}}$单调递增，
当$x > 1$时，$h^{'} ( x ) < 0$，$h ( x ) = \frac{4 x}{e^{x}}$单调递减，
故$h ( x ) = \frac{4 x}{e^{x}}$在$x = 1$处取得极大值，也是最大值，
故$h {( x ) ( 1 ) \frac{4}{e}}_{\max}$，
所以$a \geq \frac{4}{e}$，故*a*的取值范围为$\left. \frac{4}{e} , + \infty \right)$.
##### 3．（2023下·重庆永川·高二重庆市永川北山中学校校考阶段练习）已知函数$f ( x ) = e^{x} - k x$，$x \in \mathbf{R}$，*k*为常数，e是自然对数的底数．
(1)当$k = e$时，求$f ( x )$的极值；
(2)若$k > 0$，且对于任意$x > 0$，$f ( x ) > 0$恒成立，试确定实数*k*的取值范围．
<span class="fake-tag">答案</span>(1)极小值为0，无极大值
(2)$0 < k < e$
<span class="fake-tag">详解</span>（1）当$k = e$时，$f ( x ) = e^{x} - e x$，∴$f^{'} ( x ) = e^{x} - e$，
由$f^{'} ( x ) > 0$得$x > 1$，故$f ( x )$的单调递增区间为$( 1 , + \infty )$；由$f^{'} ( x ) < 0$得$x < 1$，
故$f ( x )$的单调递减区间为；
所以函数有极小值为$f ( 1 ) = e - e = 0$，无极大值．
（2）当$x > 0$时，不等式化简为，令$g ( x ) = \frac{e^{x}}{x}$，则$g^{'} ( x ) = \frac{e^{x} ( x - 1 )}{x^{2}}$；
令$g^{'} ( x ) > 0$得$x > 1$，
∴$g ( x )$在上单调递减，在$( 1 , + \infty )$上单调递增；
因为$g ( x )_{\text{min}} = g ( 1 ) = e$，所以$k < e$，
又$k > 0$，所以$0 < k < e$．
##### 4．（2023上·广东佛山·高三统考阶段练习）已知函数$f ( x ) = e^{x} - a ( x + 1 )$．
(1)求函数$f ( x )$的单调区间；
(2)当$x \in ( - 1 , + \infty )$时，$f ( x ) \geq 0$恒成立，求实数$a$的取值范围．
<span class="fake-tag">答案</span>(1)答案见解析；
(2)$\left. - \infty , 1 \right.$.
<span class="fake-tag">详解</span>（1）对函数$f ( x )$求导可得$f^{'} ( x ) = e^{x} - a$．
当$a \leq 0$时，$f^{'} ( x ) > 0$恒成立，可得函数$f ( x )$的单调递增区间为$R$，无单调递减区间；
当$a > 0$时，令$f^{'} ( x ) < 0$，可得$x < \ln a$，可得函数$f ( x )$的单调递减区间为$\left( - \infty , \ln a \right)$；
令$f^{'} ( x ) > 0$，可得$x > \ln a$，可得函数$f ( x )$的单调递增区间为$\left( \ln a , + \infty \right)$；
综上所述：$a \leq 0$时，$f ( x )$的单调递增区间为$R$，无单调递减区间；$a > 0$时，$f ( x )$的单调递增区间为$\left( \ln a , + \infty \right)$，单调递减区间为$\left( - \infty , \ln a \right)$；
（2）法一：当$x \in ( - 1 , + \infty )$时，$f ( x ) \geq 0$恒成立等价于$a \leq \frac{e^{x}}{x + 1}$，
令$g ( x ) = \frac{e^{x}}{x + 1}$，则$g^{'} ( x ) = \frac{e^{x} \cdot x}{( x + 1 )^{2}}$，
令$g^{'} ( x ) < 0$，可得$x \in ( - 1 , 0 )$，即有$g ( x )$在$( - 1 , 0 )$上单调递减；
令$g^{'} ( x ) > 0$，可得$x \in ( 0 , + \infty )$，即有$g ( x )$在$( 0 , + \infty )$上单调递增．
从而可得函数$g ( x )$的最小值为$g ( 0 ) = 1$，
综上即可得$a$的取值范围是$\left. - \infty , 1 \right.$．
法二：由（1）知，当$a \leq 0$时，函数$f ( x )$在上单调递增，
所以$f ( x ) > f ( - 1 ) = e^{- 1} > 0$满足题意；
当$0 < a \leq e^{- 1}$时，$\text{ln} a \leq - 1$，所以函数$f ( x )$的在上单调递增，
所以$f ( x ) > f ( - 1 ) = e^{- 1} > 0$满足题意；
当$a > e^{- 1}$时，$\text{ln} a > - 1$，函数$f ( x )$的在$\left( - 1 , \text{ln} a \right)$上单调递减，在$\left( \ln a , + \infty \right)$上单调递增，
所以$f ( x )_{\text{min}} = f \left( \text{ln} a \right) = a - a \left( \text{ln} a + 1 \right) = - a \text{ln} a$，
因为$f ( x ) \geq 0$，所以$f ( x )_{\text{min}} \geq 0$，即$- a \text{ln} a \geq 0 \Rightarrow \ln a \leq 0$，解得：$e^{- 1} < a \leq 1$，
综上，实数$a$的取值范围是$\left. - \infty , 1 \right.$．
##### 5．（2023上·广东梅州·高三校考阶段练习）已知函数，$g ( x ) = a x^{3} + b x^{2} + c x + d ( a \neq 0 )$是$R$上的奇函数，当$x = 1$时，$g ( x )$取得极值$- 2$．
(1)求函数$g ( x )$的单调区间和极大值；
(2)若对任意$x \in [ - 1 , 3 ]$，都有$f ( x ) \leq g ( x )$成立，求实数$k$的取值范围；
<span class="fake-tag">答案</span>(1)单调增区间为$( - \infty , - 1 )$和，单调减区间为，极大值为2
(2)$\left. 8 , + \infty )$
<span class="fake-tag">详解</span>（1）是$R$上的奇函数，
，即$a ( - x )^{3} + b ( - x )^{2} + c ( - x ) + d = - \left( a x^{3} + b x^{2} + c x + d \right)$，
得$b x^{2} + d = 0$恒成立，
可得$b = d = 0$，即，$g^{'} ( x ) = 3 a x^{2} + c$，
又当$x = 1$时，$g ( x )$取得极值$- 2$，$\begin{cases}g^{'} ( 1 ) = 3 a + c = 0 \\ g ( 1 ) = a + c = - 2\end{cases}$，
解得$\begin{cases}a = 1 \\ c = - 3\end{cases}$，故函数$g ( x ) = x^{3} - 3 x$，导函数$g^{'} ( x ) = 3 x^{2} - 3$，
令$3 x^{2} - 3 = 0$解得$x = \pm 1$，当$x \in ( - \infty , - 1 ) \cup ( 1 , + \infty )$，$g^{'} ( x ) > 0$，
当$x \in ( - 1 , 1 )$时，$g^{'} ( x ) < 0$，
$g ( x )$单调增区间为$( - \infty , - 1 )$和，单调减区间为，
故当$x = - 1$时，$g ( x )$取到极大值；
（2），对任意$x \in [ - 1 , 3 ]$，
都有$f ( x ) \leq g ( x )$成立，只需$k \geq - x^{3} + 2 x^{2} + 4 x$在$x \in [ - 1 , 3 ]$时恒成立，
构造函数$F ( x ) = - x^{3} + 2 x^{2} + 4 x$，$x \in [ - 1 , 3 ]$，则有$F^{'} ( x ) = - 3 x^{2} + 4 x + 4$，
令可得或$x = - \frac{2}{3}$，当$x \in \left( - 1 , - \frac{2}{3} \right)$时，$F^{'} ( x ) < 0$，$F ( x )$单调递减，
当$x \in \left( - \frac{2}{3} , 2 \right)$时，$F^{'} ( x ) > 0$，$F ( x )$单调递增，
当时，$F^{'} ( x ) < 0$，$F ( x )$单调递减，
当时，$F ( x )$取到极大值$F ( 2 ) = 8$，又，
故$F ( x )$的最大值为8，
故实数$k$的取值范围为：$\left. 8 , + \infty )$.
方法二：分类讨论法
##### 1．（2023·高二校考课时练习）已知不等式$e^{x} \geq ( 2 a - 3 ) x$对任意$x \in \mathbf{R}$恒成立，则实数$a$的最大值是 .
<span class="fake-tag">答案</span>$\frac{e + 3}{2}$
<span class="fake-tag">详解</span>不等式$e^{x} \geq ( 2 a - 3 ) x$对任意$x \in \mathbf{R}$恒成立，即$e^{x} - ( 2 a - 3 ) x \geq 0$对任意$x \in \mathbf{R}$恒成立，
设$f ( x ) = e^{x} - ( 2 a - 3 ) x$，$f^{'} ( x ) = e^{x} - ( 2 a - 3 )$，
$2 a - 3 < 0$时，$f^{'} ( x ) > 0$在$R$上恒成立，$f ( x )$在$R$上单调递增，无最小值，
函数$y = e^{x}$和函数$y = - ( 2 a - 3 ) x$在$R$上都单调递增，$e^{x} \in ( 0 , + \infty )$，$- ( 2 a - 3 ) x \in ( - \infty , + \infty )$，$f ( x ) \geq 0$不恒成立.
$2 a - 3 = 0$时，$f ( x ) = e^{x} \geq 0$恒成立，此时$a = \frac{3}{2}$，
时，$f^{'} ( x ) < 0$解得$x < \ln ( 2 a - 3 )$，$f^{'} ( x ) > 0$解得$x > \ln ( 2 a - 3 )$，
$f ( x )$在$\left( - \infty , \ln ( 2 a - 3 ) \right)$上单调递减，在$\left( \ln ( 2 a - 3 ) , + \infty \right)$上单调递增，
当$x = \ln ( 2 a - 3 )$时，$f ( x )$有最小值$f \left( \ln ( 2 a - 3 ) \right) = e^{\ln ( 2 a - 3 )} - ( 2 a - 3 ) \ln ( 2 a - 3 ) = ( 2 a - 3 ) - ( 2 a - 3 ) \ln ( 2 a - 3 )$，
故$( 2 a - 3 ) - ( 2 a - 3 ) \ln ( 2 a - 3 ) \geq 0$，$0 < 2 a - 3 \leq e$，$\frac{3}{2} < a \leq \frac{\text{e+3}}{2}$，
综上可知，实数$a$的最大值为$\frac{e + 3}{2}$.
故答案为：$\frac{e + 3}{2}$.
##### 2．（2023·全国·高三专题练习）设函数，若当$x \geq 0$时$f ( x ) \geq 0$，求$a$的取值范围．
<span class="fake-tag">答案</span>$a \leq \frac{1}{2}$
<span class="fake-tag">详解</span>：由题得，
令$g ( x ) = e^{x} - 1 - 2 a x$，所以$g^{'} ( x ) = e^{x} - 2 a$，$x \in \left. 0 , + \infty )$
当$a \leq \frac{1}{2}$时，$g^{'} ( x ) = e^{x} - 2 a \geq 0$恒成立，仅当$a = \frac{1}{2} , x = 0$时，
在$x \in \left. 0 , + \infty )$单调递增，所以$f^{'} ( x ) \geq f^{'} ( 0 ) = 0$，
所以函数$f ( x )$在上单调递增.
所以$f ( x ) \geq f ( 0 ) = 0$满足题意；
当$a > \frac{1}{2}$时，$g^{'} ( x ) = e^{x} - 2 a > 0$得$x \in \left( \ln 2 a , + \infty \right)$，$g^{'} ( x ) = e^{x} - 2 a < 0$得$x \in \left. 0 , \ln 2 a \right)$，
所以在$x \in \left. 0 , \ln 2 a \right)$单调递减，在$x \in \left( \ln 2 a , + \infty \right)$单调递增，
又$f^{'} ( 0 ) = 0 , \therefore f^{'} ( \ln 2 a ) < 0$，所以函数$f ( x )$在$x \in \left. 0 , \ln 2 a \right)$单调递减，
又$f ( 0 ) = 0$，所以函数$f ( x )$在$x \in ( 0 , \ln 2 a )$上$f ( x ) < f ( 0 ) = 0$，与已知矛盾，不合题意，所以舍去.
综上所述：$a \leq \frac{1}{2}$.
##### 3．（2023·全国·高三专题练习）设函数$f ( x ) = \ln ( x + 1 ) - \frac{a x}{x + 1}$．
(1)讨论$f ( x )$的单调性；
(2)若$f ( x ) \geq 0$恒成立，求*a*的值．
<span class="fake-tag">答案</span>(1)答案见解析
(2)$a = 1$
<span class="fake-tag">详解</span>（1）$f ( x )$的定义域为．$f^{'} ( x ) = \frac{1}{x + 1} - \frac{a}{( x + 1 )^{2}} = \frac{x + 1 - a}{( x + 1 )^{2}}$．
若$a \leq 0$，则$f^{'} ( x ) > 0$，$f ( x )$在上单调递增．
若$a > 0$，则当$x \in ( - 1 , a - 1 )$时，$f^{'} ( x ) < 0$，$f ( x )$单调递减；
当$x \in ( a - 1 , + \infty )$时，$f^{'} ( x ) > 0$，$f ( x )$单调递增；
（2）由（1）知，若$a \leq 0$，则当$- 1 < x < 0$时，$f ( x ) < f ( 0 ) = 0$，矛盾．
因此$a > 0$．由（1）知此时$f {( x ) ( a - 1 ) \ln a}_{\min}$．
$f ( x ) \geq 0$恒成立等价于$f {( x ) \ln a}_{\min}$恒成立．
设$g ( a ) = \ln a - a + 1 ( a > 0 )$，即$g ( a ) \geq 0$恒成立，
则$g^{'} ( a ) = \frac{1 - a}{a}$，
当$x \in ( 0 , 1 )$时，$g^{'} ( a ) > 0 , g ( a )$单调递增，
当$x \in ( 1 , + \infty )$时，$g^{'} ( a ) < 0 , g ( a )$单调递减，所以$g {( a ) ( 1 )}_{\max}$，
显然函数$g ( a )$在$a = 1$处有唯一零点，且$g ( a ) \leq 0$．
而$g ( a ) \geq 0$恒成立，所以$g ( a ) = 0$，
所以$a = 1$．
##### 4．（2021上·广东深圳·高三深圳市福田区福田中学校考阶段练习）已知函数$f ( x ) = \frac{1}{2} x^{2} - ( a + 1 ) \ln x - \frac{1}{2} ( a \in \mathbf{R} , a \neq 0 )$．
（1）讨论函数的单调性；
（2）若对任意的$x \in \lbrack 1 , + \infty )$，都有$f ( x ) \geq 0$成立，求$a$的取值范围．
<span class="fake-tag">答案</span>（1）答案见解析；（2）$a \leq 0$.
<span class="fake-tag">详解</span>解：（1）由已知定义域为$( 0 , + \infty )$，$f ' ( x ) = x - \frac{a + 1}{x} = \frac{x^{2} - ( a + 1 )}{x}$
当$a + 1 \leq 0$，即$a \leq - 1$时，$f ' ( x ) > 0$恒成立，则$f ( x )$在$( 0 , + \infty )$上单调递增；
当$a + 1 > 0$，即$a > - 1$时，$x = - \sqrt{a + 1}$（舍）或$x = \sqrt{a + 1}$，所以$f ( x )$在$\left( 0 , \sqrt{a + 1} \right)$上单调递减，在$\left( \sqrt{a + 1} , + \infty \right)$上单调递增.
所以$a \leq - 1$时，$f ( x )$在$( 0 , + \infty )$上单调递增；
$a > - 1$时，$f ( x )$在$\left( 0 , \sqrt{a + 1} \right)$上单调递减，在$\left( \sqrt{a + 1} , + \infty \right)$上单调递增.
（2）由（1）可知，当$a \leq - 1$时，$f ( x )$在$( 1 , + \infty )$上单调递增，若$f ( x ) \geq 0$对任意的$x \in \lbrack 1 , + \infty )$恒成立，只需$f ( 1 ) \geq 0$，而$f ( 1 ) = 0$恒成立，所以$a \leq - 1$成立；
当$a > - 1$时，若$\sqrt{a + 1} \leq 1$，即$- 1 < a \leq 0$，则$f ( x )$在$( 1 , + \infty )$上单调递增，又$f ( 1 ) = 0$，所以$- 1 < a \leq 0$成立；
若$a > 0$，则$f ( x )$在$\left( 1 , \sqrt{a + 1} \right)$上单调递减，在$\left( \sqrt{a + 1} , + \infty \right)$上单调递增，又$f ( 1 ) = 0$，所以$\exists x_{0} \in \left( 1 , \sqrt{a + 1} \right)$，$f ( x_{0} ) < f ( 1 ) = 0$，不满足$f ( x ) \geq 0$对任意的$x \in \lbrack 1 , + \infty )$恒成立.
所以综上所述：$a \leq 0$.
方法三：等价转化法
##### 1．（2023下·海南省直辖县级单位·高二嘉积中学校考期中）已知函数$f ( x ) = ( a - 1 ) \ln x + x + \frac{a}{x}$，$g ( x ) = \frac{a}{x}$（其中$a \in \mathbf{R}$）．
(1)若$a = - 2$，求函数$f ( x )$的单调区间；
(2)若对于任意$x \in \left. 1 , e \right.$，都有$f ( x ) > g ( x )$成立，求$a$的取值范围．
<span class="fake-tag">答案</span>(1)单调增区间为$( 0 , 1 )$和$( 2 , + \infty )$，单调减区间为$( 1 , 2 )$
(2)$( 1 - e , + \infty )$
<span class="fake-tag">详解</span>（1）若$a = - 2$，则$f ( x ) = - 3 \ln x + x - \frac{2}{x} ( x > 0 )$，
$f^{'} ( x ) = - \frac{3}{x} + 1 + \frac{2}{x^{2}} = \frac{x^{2} - 3 x + 2}{x^{2}} = \frac{( x - 1 ) ( x - 2 )}{x^{2}}$，
令$f^{'} ( x ) > 0$，可得$0 < x < 1$或$x > 2$，令$f^{'} ( x ) < 0$，可得$1 < x < 2$，
所以单调增区间为$( 0 , 1 )$和$( 2 , + \infty )$，单调减区间为$( 1 , 2 )$.
（2）因为对于任意$x \in \left. 1 , e \right.$，都有$f ( x ) > g ( x )$成立，
所以对于任意$x \in \left. 1 , e \right.$，都有$f ( x ) - g ( x ) > 0$成立,
即对于任意$x \in \left. 1 , e \right.$，$( a - 1 ) \ln x + x > 0$；
因为$\ln x > 0$，所以对于任意$x \in \left. 1 , e \right.$，$( a - 1 ) > \frac{- x}{\ln x}$.
设$h ( x ) = \frac{- x}{\ln x}$，其中$x \in \left. 1 , e \right.$，则$h^{'} ( x ) = \frac{- \ln x + 1}{( \ln x )^{2}}$，
因为$x \in \left. 1 , e \right.$，所以$- \ln x + 1 \geq 0$，所以$h^{'} ( x ) \geq 0$，
因此在$\left. 1 , e \right.$单调递增，所以$h ( x {) e e}_{\max}$，
所以$a - 1 > - e$，即$a > 1 - e$，故$a$的取值范围为$( 1 - e , + \infty )$.
##### 2．（2022下·福建泉州·高二福建省泉州市培元中学校考期中）已知函数$f ( x ) = e^{x} - \frac{1}{2} x^{2} , g ( x ) = a x + 1 ( a \in R )$.
(1)求$f ( x )$的图象在$x = 0$处的切线方程；
(2)当$x \in \left. 0 , + \infty )$时，$f ( x ) \geq g ( x )$恒成立，求$a$的取值范围.
<span class="fake-tag">答案</span>(1)$x - y + 1 = 0$
(2)$\left. - \infty , 1 \right.$
<span class="fake-tag">详解</span>（1）函数$f ( x ) = e^{x} - \frac{1}{2} x^{2} , f ( 0 ) = 1$，切点为，
$f^{'} ( x ) = e^{x} - x$，∴$k = f^{'} ( 0 ) = 1$，
∴$f ( x )$的图象在$x = 0$处的切线方程为：$y - 1 = x$，即$x - y + 1 = 0$.
（2）令$h ( x ) = f ( x ) - g ( x ) = e^{x} - \frac{1}{2} x^{2} - a x - 1$，$h ( 0 ) = 0$.
$h^{'} ( x ) = e^{x} - x - a$，设$k ( x ) = h^{'} ( x ) = e^{x} - x - a$，$k^{'} ( x ) = e^{x} - 1$，
∵$x \in \left. 0 , + \infty )$，∴，$k ( x )$在$x \in \left. 0 , + \infty )$上单调递增，
即$h^{'} ( x )$在$x \in \left. 0 , + \infty )$上单调递增，$h^{'} ( 0 ) = 1 - a$，
当时，$h^{'} ( x ) \geq 1 - a \geq 0$，∴$h ( x )$在$x \in \left. 0 , + \infty )$上单调递增，
∴$h ( x ) \geq h ( 0 ) = 0$，
∴当$x \in [ 0 , + \infty ) \right.$时，$f ( x ) \geq g ( x )$恒成立.
当$a > 1$时，$h^{'} ( 0 ) = 1 - a < 0$，
∵函数$y = e^{x} - x - a$在$( 0 , + \infty )$上存在唯一的零点$x_{0}$，
∴函数$h ( x )$在区间$\left( 0 , x_{0} \right)$上单调递减，$h \left( x_{0} \right) < h ( 0 ) = 0$，不符合题意，舍去.
综上可得：$a$的取值范围是$\left. - \infty , 1 \right.$.
##### 3．（2023上·北京丰台·高三统考期中）已知函数$f ( x ) = \ln ( x + 1 )$，$g ( x ) = k x$．
(1)当$k = 1$时，求函数$h ( x ) = f ( x ) - g ( x )$的最大值；
(2)若关于$x$的不等式$f ( x ) \leq g ( x )$恒成立，求实数$k$的值．
<span class="fake-tag">答案</span>(1)0
(2)1
<span class="fake-tag">详解</span>（1）当$k = 1$时，函数$h ( x ) = f ( x ) - g ( x ) = \ln ( x + 1 ) - x$，
易知，$x > - 1$，$h^{'} ( x ) = \frac{1}{x + 1} - 1 = \frac{- x}{x + 1}$，
当$x \in ( - 1 , 0 )$，$h^{'} ( x ) > 0$，当$x \in ( 0 , + \infty )$，，
即$h ( x ) = \ln ( x + 1 ) - x$在区间$( - 1 , 0 )$上单调递增，在区间$( 0 , + \infty )$上单调递减，
故最大值为$h ( 0 ) = \ln 1 - 0 = 0$.
（2）令$h ( x ) = f ( x ) - g ( x ) = \ln ( x + 1 ) - k x ( x > - 1 )$
则$h^{'} ( x ) = \frac{1}{x + 1} - k = \frac{1 - k x - k}{x + 1} = \frac{1 - k x - k}{x + 1} = \frac{- k x + 1 - k}{x + 1}$，
当$k = 0$时，由$f ( x ) \leq g ( x )$，即$\ln ( x + 1 ) \leq 0$，得到$- 1 < x < 0$，显然不合题意，故$k \neq 0$，
由$h^{'} ( x ) = 0$，得到$x = \frac{1}{k} - 1$，故$h^{'} ( x ) = \frac{- k ( x + 1 - \frac{1}{k} )}{x + 1}$
当$k > 1$时，$x \in ( - 1 , \frac{1}{k} - 1 )$时，$h^{'} ( x ) > 0$，$x \in ( \frac{1}{k} - 1 , + \infty )$时，，
即$k > 1$时，函数$h ( x ) = \ln ( x + 1 ) - k x$在区间$( - 1 , \frac{1}{k} - 1 )$单调递增，在区间$( \frac{1}{k} - 1 , + \infty )$上单调递减，
又$h ( 0 ) = \ln ( 0 + 1 ) - k \times 0 = 0$，$\frac{1}{k} - 1 < 0$，所以，当$x \in ( \frac{1}{k} - 1 , 0 )$时，$h ( x ) > 0$，即$f ( x ) > g ( x )$，故$k > 1$时，不满足$f ( x ) \leq g ( x )$恒成立，
由（1）知当$k = 1$时，$h ( x ) = f ( x ) - g ( x ) \leq 0$恒成立，即$f ( x ) \leq g ( x )$恒成立，
当$0 < k < 1$时，$x \in ( - 1 , \frac{1}{k} - 1 )$时，$h^{'} ( x ) > 0$，$x \in ( \frac{1}{k} - 1 , + \infty )$时，，
即$0 < k < 1$时，函数$h ( x ) = \ln ( x + 1 ) - k x$在区间$( - 1 , \frac{1}{k} - 1 )$单调递增，在区间$( \frac{1}{k} - 1 , + \infty )$上单调递减，
又$h ( 0 ) = \ln ( 0 + 1 ) - k \times 0 = 0$，$\frac{1}{k} - 1 > 0$，所以，当$x \in ( 0 , \frac{1}{k} - 1 )$时，$h ( x ) > 0$，即$f ( x ) > g ( x )$，故$0 < k < 1$时，不满足$f ( x ) \leq g ( x )$恒成立，
当$k < 0$，$h^{'} ( x ) = \frac{1}{x + 1} - k > 0$恒成立，即在区间$( 0 , + \infty )$上单调递增，
又$h ( 0 ) = \ln ( 0 + 1 ) - k \times 0 = 0$，所以，当$x \in ( 0 , + \infty )$时，$h ( x ) > 0$，即$f ( x ) > g ( x )$，故$k < 0$时，不满足$f ( x ) \leq g ( x )$恒成立，
综上所述，实数$k$的值为$1$.
##### 4．（2023·全国·高三专题练习）已知函数$f ( x ) = x^{2} + ( a + 1 ) x - \ln x$，$g ( x ) = x^{2} + x + 2 a + 1$．
(1)若$f ( x )$在$( 1 , + \infty )$上单调递增，求实数*a*的取值范围；
(2)当$x \in \left. 1 , e )$时，$f ( x ) < g ( x )$恒成立，求实数*a*的取值范围．
<span class="fake-tag">答案</span>(1)$\left\{ \left. a | a \geq - 2 \right\}$
(2)$\left\{ \left. a | - 1 < a < \frac{2}{e - 2} \right\}$
<span class="fake-tag">详解</span>（1）$f ( x ) = x^{2} + ( a + 1 ) x - \ln x$，$f^{'} ( x ) = 2 x + ( a + 1 ) - \frac{1}{x}$．
依题意知$x \in ( 1 , + \infty )$时，$2 x + ( a + 1 ) - \frac{1}{x} \geq 0$恒成立，即$a + 1 \geq \frac{1}{x} - 2 x$．
令$k ( x ) = \frac{1}{x} - 2 x$，$x \in ( 1 , + \infty )$，
∴$k^{'} ( x ) = - \frac{1}{x^{2}} - 2 < 0$，
∴$k ( x ) = \frac{1}{x} - 2 x$在$x \in ( 1 , + \infty )$上单调递减，
∴$k ( x ) < k ( 1 ) = - 1$，
∴$a + 1 \geq - 1$，解得$a \geq - 2$，
∴实数*a*的取值范围为$\left\{ \left. a | a \geq - 2 \right\}$；
（2）令$\varphi ( x ) = f ( x ) - g ( x ) = a x - \ln x - 2 a - 1$，$x \in \left. 1 , e )$，则只需$\varphi ( x )_{\max}$即可，
∴$\varphi^{'} ( x ) = a - \frac{1}{x} = \frac{a x - 1}{x}$．
当$a \leq 0$时，$\varphi^{'} ( x ) < 0$，
∴$\varphi ( x )$在$x \in \left. 1 , e )$上单调递减，
∴$\varphi {( x ) ( 1 )}_{\max}$，
∴$- a - 1 < 0$，即$a > - 1$，∴$- 1 < a \leq 0$．
当$a > 0$时，当$x \in \left( 0 , \frac{1}{a} \right)$时，$\varphi^{'} ( x ) < 0$，当$x \in \left( \frac{1}{a} , + \infty \right)$时，$\varphi^{'} ( x ) > 0$，
∴$\varphi ( x )$在$x \in \left( 0 , \frac{1}{a} \right)$上单调递减，在$x \in \left( \frac{1}{a} , + \infty \right)$上单调递增，
∴要使$\varphi ( x )_{\max}$，
只需$\begin{cases}\varphi ( 1 ) < 0 \\ \varphi ( e ) < 0 \\ a > 0\end{cases}$，即$\begin{cases}- a - 1 < 0 \\ a e - 2 a - 2 < 0 \\ a > 0\end{cases}$，解得$0 < a < \frac{2}{e - 2}$，
综上，实数*a*的取值范围为$\left\{ \left. a | - 1 < a < \frac{2}{e - 2} \right\}$．
##### 5．（2023·全国·高三专题练习）已知函数$f ( x ) = a e^{x} + \ln a$，$g ( x ) = \ln ( x + 1 ) + 1$（其中$a$为常数，$e$为自然对数底数）.若$f ( x ) > g ( x )$恒成立，求$a$的取值范围．
<span class="fake-tag">答案</span>$a > 1$
<span class="fake-tag">详解</span>由$f ( x ) > g ( x )$可知，$a e^{x} + \ln a > \ln ( x + 1 ) + 1 ( a > 0 , x > - 1 )$，
令$x = 0$，有$a + \ln a > 1$，因为$y = x + \ln x$单调递增，$1 + \ln 1 = 1$，所以$a > 1$，
当$a > 1$时，$a e^{x} + \ln a > e^{x}$，
令$h ( x ) = e^{x} - x - 1$，则$h^{'} ( x ) = e^{x} - 1$，
令$h^{'} ( x ) > 0$，解得$x > 0$，令$h^{'} ( x ) < 0$，解得$x < 0$，
所以$h ( x )$在$( - \infty , 0 )$上单调递减，$( 0 , + \infty )$上单调递增，$h {( x ) ( 0 )}_{\min}$，
所以$e^{x} - x - 1 \geq 0$，即；
令$F ( x ) = \ln ( x + 1 ) - x$，定义域为，则$F^{'} ( x ) = \frac{1}{x + 1} - 1 = \frac{- x}{x + 1}$，
令$F^{'} ( x ) > 0$，则$- 1 < x < 0$，令$F^{'} ( x ) < 0$，则$x > 0$，
所以$F ( x )$在$( - 1 , 0 )$上单调递增，$( 0 , + \infty )$上单调递减，$F {( x ) ( 0 )}_{\max}$，
所以$\ln ( x + 1 ) - x \leq 0$，即$\ln ( x + 1 ) \leq x$
故，即$a e^{x} + \ln a > \ln ( x + 1 ) + 1$成立，即$a > 1$．
